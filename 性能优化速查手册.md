# 性能优化速查手册

## 快速问题诊断和解决方案

> 本手册提供快速查找表和决策树，帮助你在5分钟内定位性能问题并找到解决方向

---

## 📋 目录

1. [5分钟快速诊断流程](#1-5分钟快速诊断流程)
2. [性能指标速查表](#2-性能指标速查表)
3. [工具使用速查](#3-工具使用速查)
4. [问题-解决方案速查](#4-问题解决方案速查)
5. [优化技巧速查](#5-优化技巧速查)
6. [常见错误和反模式](#6-常见错误和反模式)

---

## 1. 5分钟快速诊断流程

### Step 1: 使用top/htop快速观察 (30秒)

```bash
htop
# 观察:
# 1. CPU使用率: 接近100%? → CPU bound
# 2. 内存使用: 接近满? → 内存问题
# 3. wa%: 高? → IO bound
# 4. 进程状态: D状态多? → IO等待
```

**决策树**:

```
CPU使用率?
├─ > 90%  → 跳到 Step 2A (CPU分析)
├─ < 50%  → 跳到 Step 2B (IO/内存分析)
└─ 50-90% → 可能是混合瓶颈，先查CPU
```

### Step 2A: CPU瓶颈快速分析 (2分钟)

```bash
# 找热点函数
perf top -g

# 观察:
# 1. 哪个函数占比最高?
# 2. 是用户态还是内核态?
# 3. 是否在做大量系统调用?

# 快速决策:
# • 用户态高 (>80%) → 代码优化
# • 内核态高 (>30%) → 系统调用优化
# • 锁函数占比高 (>10%) → 锁竞争
```

**常见热点模式识别**:

```
热点特征                      原因              解决方向
────────────────────────────────────────────────────────
malloc/free高                内存分配          对象池/arena
memcpy/memset高              数据拷贝          减少拷贝/零拷贝
__strcmp/__strlen高          字符串操作        优化算法
_spin_lock高                 锁竞争            无锁/RCU/分段锁
page_fault                   缺页              大页/预分配
__schedule                   上下文切换        减少线程/协程
```

### Step 2B: IO/内存瓶颈快速分析 (2分钟)

```bash
# IO分析
iostat -x 1 5

# 观察:
# %util 接近100% → IO饱和
# await > 10ms → IO延迟高
# r/s, w/s → IOPS

# 内存分析
free -h
vmstat 1 5

# 观察:
# available < 10% → 内存不足
# si/so > 0 → 发生swap
# buff/cache很大 → 正常
```

**快速决策**:

```
IO问题?
├─ IOPS饱和 → 减少IO次数/异步IO/SSD
├─ 延迟高 → 检查磁盘健康/使用Direct IO
└─ 顺序IO → 调整预读/使用大块IO

内存问题?
├─ Swap活跃 → 增加内存/减少内存使用/优化算法
├─ 内存泄漏 → valgrind/内存profiler
└─ 缓存miss高 → 数据布局优化/预取
```

### Step 3: 深入分析 (2分钟)

```bash
# CPU详细分析
perf stat -e cycles,instructions,cache-misses,branch-misses ./program

# 关键指标:
IPC = instructions / cycles

# 判断:
IPC < 0.5  → 严重停顿 (缓存/分支)
IPC 0.5-1.5 → 一般 (有优化空间)
IPC 1.5-3.0 → 良好 (接近硬件极限)
IPC > 3.0   → 优秀 (可能已是最优)

# 缓存分析
Cache Miss Rate = cache-misses / cache-references

< 5%  → 良好
5-15% → 可优化
> 15% → 严重问题

# 分支预测
Branch Miss Rate = branch-misses / branches

< 1%  → 优秀
1-5%  → 良好
5-10% → 可优化
> 10% → 严重问题
```

---

## 2. 性能指标速查表

### 2.1 CPU性能指标

| 指标 | 优秀 | 良好 | 需优化 | 严重问题 | 优化方向 |
|------|------|------|--------|----------|----------|
| IPC | >2.0 | 1.5-2.0 | 1.0-1.5 | <1.0 | 缓存/分支优化 |
| L1 Cache Miss | <2% | 2-5% | 5-10% | >10% | 数据布局 |
| LLC Cache Miss | <1% | 1-5% | 5-15% | >15% | 工作集/分块 |
| Branch Miss | <1% | 1-5% | 5-10% | >10% | 分支优化 |
| TLB Miss | <0.1% | 0.1-0.5% | 0.5-1% | >1% | 大页 |
| Context Switch | <100/s | 100-1000/s | 1K-10K/s | >10K/s | 减少线程 |

### 2.2 内存性能指标

| 指标 | 优秀 | 良好 | 需注意 | 问题 | 解决方案 |
|------|------|------|--------|------|----------|
| 内存使用率 | <70% | 70-85% | 85-95% | >95% | 增加内存/优化 |
| Swap使用 | 0 | 0 | <10% | >10% | 禁用swap/加内存 |
| Page Fault | <100/s | 100-1000/s | 1K-10K/s | >10K/s | 预分配/大页 |
| NUMA命中率 | >99% | 95-99% | 90-95% | <90% | NUMA绑定 |

### 2.3 IO性能指标

| 指标 | 优秀 | 良好 | 需优化 | 瓶颈 | 解决方案 |
|------|------|------|--------|------|----------|
| IOPS (HDD) | - | 100-200 | <100 | 饱和 | 换SSD |
| IOPS (SATA SSD) | - | 50K-90K | <50K | 饱和 | 换NVMe |
| IOPS (NVMe) | - | 200K-1M | <200K | - | 检查配置 |
| IO延迟 (HDD) | <5ms | 5-10ms | 10-20ms | >20ms | 换SSD |
| IO延迟 (SSD) | <0.1ms | 0.1-1ms | 1-10ms | >10ms | 检查健康度 |
| %util | <70% | 70-90% | 90-95% | >95% | 增加磁盘/优化 |

### 2.4 网络性能指标

| 指标 | 优秀 | 良好 | 需优化 | 瓶颈 | 解决方案 |
|------|------|------|--------|------|----------|
| 带宽利用率 | <70% | 70-85% | 85-95% | >95% | 升级带宽 |
| 网络延迟 | <1ms | 1-5ms | 5-20ms | >20ms | 检查路由 |
| 丢包率 | 0% | <0.01% | 0.01-0.1% | >0.1% | 检查硬件 |
| TCP重传率 | <0.5% | 0.5-2% | 2-5% | >5% | 调优TCP参数 |

---

## 3. 工具使用速查

### 3.1 perf命令速查

```bash
# ===== 基础命令 =====

# 实时热点 (最常用)
perf top -g

# 录制和分析
perf record -g ./program          # 录制
perf report                        # 查看报告
perf annotate function_name        # 汇编级分析

# ===== 性能统计 =====

# 基础统计
perf stat ./program

# 详细统计
perf stat -e cycles,instructions,cache-misses,branch-misses ./program

# 缓存详细分析
perf stat -e L1-dcache-loads,L1-dcache-load-misses,\
    LLC-loads,LLC-load-misses ./program

# ===== 高级分析 =====

# 采样频率调整
perf record -F 999 -g ./program   # 999 Hz采样

# 针对特定CPU
perf record -C 0-3 -g ./program   # CPU 0-3

# 针对特定进程
perf record -p <PID> -g           # 附加到进程

# 系统级录制
perf record -a -g                 # 所有CPU

# ===== 火焰图 =====

git clone https://github.com/brendangregg/FlameGraph
perf record -F 99 -ag -- sleep 60
perf script | ./FlameGraph/stackcollapse-perf.pl | \
    ./FlameGraph/flamegraph.pl > flamegraph.svg

# ===== 内存分析 =====

# 页面错误
perf stat -e page-faults,minor-faults,major-faults ./program

# NUMA分析
perf stat -e node-loads,node-load-misses,\
    node-stores,node-store-misses ./program

# ===== 锁分析 =====

perf lock record ./program
perf lock report
```

### 3.2 编译器优化速查

```bash
# ===== GCC/Clang优化级别 =====

# 开发阶段
-O0 -g                    # 无优化+调试信息

# 生产阶段
-O2                       # 推荐 (平衡)
-O3                       # 激进优化
-Ofast                    # 最快 (不严格遵守标准)

# ===== 架构优化 =====

-march=native             # 针对本机CPU
-march=skylake            # 针对Skylake
-march=znver2             # 针对AMD Zen2

# ===== SIMD =====

-mavx2                    # 启用AVX2
-mavx512f                 # 启用AVX-512
-mfma                     # 启用FMA

# ===== LTO =====

-flto                     # 链接时优化
-flto=thin                # Clang thin LTO (更快)

# ===== PGO =====

# Step 1: 生成profile
-fprofile-generate

# Step 2: 使用profile
-fprofile-use

# ===== 报告 =====

-fopt-info-vec-optimized  # 向量化报告
-Rpass=.*                 # LLVM优化报告 (Clang)

# ===== 完整优化命令 =====

g++ -O3 -march=native -flto -fprofile-use \
    -ffast-math -funroll-loops \
    program.cpp -o program
```

### 3.3 系统调优速查

```bash
# ===== CPU =====

# 查看CPU信息
lscpu
cat /proc/cpuinfo

# 设置CPU频率
# 性能模式
echo performance | sudo tee /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor

# 禁用超线程 (如果需要)
echo off | sudo tee /sys/devices/system/cpu/smt/control

# ===== 内存 =====

# 查看内存
free -h
vmstat 1

# 大页设置
# 临时
echo 1024 | sudo tee /proc/sys/vm/nr_hugepages

# 永久 (/etc/sysctl.conf)
vm.nr_hugepages = 1024

# 禁用透明大页
echo never | sudo tee /sys/kernel/mm/transparent_hugepage/enabled

# ===== NUMA =====

# 查看NUMA拓扑
numactl --hardware

# 绑定程序到节点
numactl --cpubind=0 --membind=0 ./program

# ===== IO =====

# 调度器 (SSD推荐none/mq-deadline)
echo none | sudo tee /sys/block/sda/queue/scheduler

# 预读设置
blockdev --setra 8192 /dev/sda  # 4MB预读

# ===== 网络 =====

# TCP优化 (/etc/sysctl.conf)
net.core.rmem_max = 134217728
net.core.wmem_max = 134217728
net.ipv4.tcp_rmem = 4096 87380 67108864
net.ipv4.tcp_wmem = 4096 65536 67108864
net.ipv4.tcp_congestion_control = bbr
net.core.somaxconn = 4096

# 应用设置
sudo sysctl -p
```

---

## 4. 问题-解决方案速查

### 4.1 CPU问题

| 症状 | 原因 | 快速诊断 | 解决方案 |
|------|------|----------|----------|
| CPU 100%但IPC<1 | 缓存miss | perf stat查看cache-misses | 数据布局优化/分块 |
| 单核100%其他闲置 | 串行瓶颈 | 检查算法 | 并行化/异步 |
| 所有核心100% | 计算密集 | 查看hot functions | 算法优化/SIMD |
| CPU低但程序慢 | IO等待 | iostat/iotop | 异步IO/缓存 |
| 频繁上下文切换 | 线程过多 | vmstat查看cs | 减少线程/协程 |
| 分支miss高 | 不可预测分支 | perf stat | 排序/消除分支 |

### 4.2 内存问题

| 症状 | 原因 | 快速诊断 | 解决方案 |
|------|------|----------|----------|
| 内存使用持续增长 | 内存泄漏 | valgrind --leak-check | 修复泄漏 |
| 频繁page fault | 内存不足 | vmstat查看fault | 增加内存/优化 |
| Swap活跃 | 内存超用 | free -h | 禁用swap/加内存 |
| 高L3 miss | 工作集太大 | perf stat | 减少工作集/分块 |
| NUMA miss高 | 跨节点访问 | numastat | NUMA绑定 |
| TLB miss高 | 虚拟内存开销 | perf stat | 使用大页 |

### 4.3 IO问题

| 症状 | 原因 | 快速诊断 | 解决方案 |
|------|------|----------|----------|
| %util 100% | IO饱和 | iostat -x | 增加磁盘/优化算法 |
| 高await | IO延迟 | iostat -x | SSD/检查硬件 |
| 小随机IO多 | 访问模式差 | blktrace | 顺序化/批量 |
| 读写比例失衡 | 未优化 | iostat -x | Direct IO/异步IO |
| 系统调用多 | 频繁IO | strace -c | 缓冲/批量 |

### 4.4 并发问题

| 症状 | 原因 | 快速诊断 | 解决方案 |
|------|------|----------|----------|
| 锁函数占比高 | 锁竞争 | perf top | 无锁/RCU/分段锁 |
| 死锁 | 锁顺序问题 | pstack/gdb | 修复锁顺序 |
| 线程扩展性差 | Amdahl定律 | 分析串行部分 | 减少串行代码 |
| False sharing | 缓存行竞争 | perf c2c | 填充到64字节 |
| 原子操作慢 | 缓存一致性 | 检查memory_order | 使用relaxed |

---

## 5. 优化技巧速查

### 5.1 算法优化

```
问题                  优化前        优化后          技巧
───────────────────────────────────────────────────────────
线性搜索              O(n)          O(log n)        二分搜索/哈希表
嵌套循环              O(n²)         O(n log n)      排序+双指针
重复计算              O(2^n)        O(n)            动态规划/记忆化
字符串比较            O(mn)         O(m+n)          KMP/哈希
```

### 5.2 数据结构优化

```
场景                  避免使用      推荐使用        原因
───────────────────────────────────────────────────────────
频繁查找              数组/链表     哈希表/树       O(1) vs O(n)
频繁插入删除          数组          链表/deque      避免移动
顺序访问              链表          数组/vector     缓存友好
范围查询              哈希表        B+树            有序性
小数据(<64B)          指针          内嵌            减少间接访问
```

### 5.3 代码级优化

```cpp
// ===== 循环优化 =====

// 反例: 循环内不变计算
for (int i = 0; i < n; i++) {
    result[i] = data[i] * (x * y);  // x*y每次计算
}

// 优化: 提取不变量
float xy = x * y;
for (int i = 0; i < n; i++) {
    result[i] = data[i] * xy;
}

// ===== 分支优化 =====

// 反例: 不可预测分支
for (int i = 0; i < n; i++) {
    if (data[i] > threshold) {  // 随机
        process(data[i]);
    }
}

// 优化: 排序后分支可预测
std::sort(data, data + n);
for (int i = 0; i < n; i++) {
    if (data[i] > threshold) {  // 可预测
        process(data[i]);
    }
}

// ===== 内存访问优化 =====

// 反例: 跨步访问
for (int j = 0; j < M; j++) {
    for (int i = 0; i < N; i++) {
        sum += matrix[i][j];  // 列优先
    }
}

// 优化: 顺序访问
for (int i = 0; i < N; i++) {
    for (int j = 0; j < M; j++) {
        sum += matrix[i][j];  // 行优先
    }
}

// ===== 函数调用优化 =====

// 反例: 虚函数开销
for (int i = 0; i < n; i++) {
    obj->virtual_method();  // 每次查vtable
}

// 优化: 批量调用
obj->batch_process(n);  // 一次虚函数调用
```

### 5.4 编译器优化提示

```cpp
// ===== 内联提示 =====

// 强制内联
__attribute__((always_inline))
inline int add(int a, int b) {
    return a + b;
}

// 禁止内联 (用于调试)
__attribute__((noinline))
int complex_function() { ... }

// ===== 分支预测提示 =====

#define likely(x)   __builtin_expect(!!(x), 1)
#define unlikely(x) __builtin_expect(!!(x), 0)

if (likely(common_case)) {
    // 热路径
} else {
    // 冷路径
}

// ===== 别名提示 =====

void process(float* __restrict__ a,
             float* __restrict__ b,
             float* __restrict__ c,
             int n) {
    // 告诉编译器a,b,c不重叠
    for (int i = 0; i < n; i++) {
        c[i] = a[i] + b[i];
    }
}

// ===== 对齐提示 =====

alignas(64) float data[1024];  // 缓存行对齐

struct alignas(64) CacheAligned {
    int value;
    char padding[60];  // 避免false sharing
};

// ===== 向量化提示 =====

#pragma omp simd
for (int i = 0; i < n; i++) {
    c[i] = a[i] + b[i];
}

#pragma GCC ivdep  // 忽略依赖
for (int i = 0; i < n; i++) {
    process(data[i]);
}
```

---

## 6. 常见错误和反模式

### 6.1 过早优化

```cpp
// ❌ 错误: 优化不是瓶颈的代码
void startup_config() {
    // 这个函数只调用一次
    // 优化它毫无意义
    for (int i = 0; i < 10; i++) {
        config[i] = load_config(i);
    }
}

// ✅ 正确: 先profile找到热点
perf record -g ./program
perf report
// 优化占比>10%的函数
```

### 6.2 盲目使用技巧

```cpp
// ❌ 错误: 认为位运算总是更快
int multiply_by_10(int x) {
    return (x << 3) + (x << 1);  // x*8 + x*2
}

// ✅ 正确: 编译器会优化
int multiply_by_10(int x) {
    return x * 10;  // 清晰且编译器优化后相同
}

// 验证: gcc -O2 -S查看汇编
```

### 6.3 忽略数据特性

```cpp
// ❌ 错误: 对已排序数据用哈希表
std::unordered_map<int, int> map;
for (auto val : sorted_data) {
    map[val] = process(val);
}

// ✅ 正确: 利用有序性
std::vector<std::pair<int, int>> result;
for (auto val : sorted_data) {
    result.emplace_back(val, process(val));
}
// 查找用二分: O(log n) vs O(1)
// 但缓存友好，实际可能更快
```

### 6.4 过度优化

```cpp
// ❌ 错误: 牺牲可读性的微优化
int sum = 0;
for (int i = 0; i < n; i += 8) {
    sum += arr[i] + arr[i+1] + arr[i+2] + arr[i+3] +
           arr[i+4] + arr[i+5] + arr[i+6] + arr[i+7];
}

// ✅ 正确: 让编译器优化
int sum = 0;
for (int i = 0; i < n; i++) {
    sum += arr[i];
}
// 用-O3编译，效果相同
```

### 6.5 忽略Amdahl定律

```cpp
// ❌ 错误: 优化占比1%的代码
void rarely_called() {
    // 这个函数占比1%
    // 优化10倍也只提升0.9%
}

// ✅ 正确: 优化占比50%的代码
void frequently_called() {
    // 这个函数占比50%
    // 优化2倍可提升33%
}
```

---

## 7. 性能优化决策树

```
遇到性能问题
│
├─【1】建立基线
│   ├─ 记录当前性能指标
│   ├─ 定义优化目标
│   └─ 确保可重现测试
│
├─【2】快速定位 (5分钟)
│   ├─ top/htop看CPU/内存
│   ├─ iostat看IO
│   └─ perf top看热点
│
├─【3】详细分析 (30分钟)
│   ├─ CPU Bound?
│   │   ├─ IPC低? → 缓存/分支优化
│   │   ├─ IPC高? → 算法/并行优化
│   │   └─ 锁竞争? → 无锁/RCU
│   │
│   ├─ Memory Bound?
│   │   ├─ Cache miss? → 数据布局
│   │   ├─ TLB miss? → 大页
│   │   └─ NUMA? → 绑定
│   │
│   ├─ IO Bound?
│   │   ├─ IOPS? → 异步/SSD
│   │   ├─ 延迟? → Direct IO
│   │   └─ 系统调用? → 批量
│   │
│   └─ 其他?
│       ├─ 网络? → 零拷贝/批量
│       ├─ 锁? → 无锁/分段
│       └─ 算法? → 复杂度优化
│
├─【4】实施优化 (因问题而异)
│   ├─ 先优化算法
│   ├─ 再优化数据结构
│   ├─ 然后代码级优化
│   └─ 最后编译器优化
│
├─【5】验证效果
│   ├─ 对比优化前后
│   ├─ 检查副作用
│   └─ 回归测试
│
└─【6】文档和监控
    ├─ 记录优化过程
    ├─ 建立性能监控
    └─ 设置性能回归检测
```

---

## 8. 快速参考卡片

### 卡片1: perf单行命令

```bash
# 热点分析
perf top -g

# 完整profiling
perf record -g ./prog && perf report

# 性能统计
perf stat -e cycles,instructions,cache-misses,branch-misses ./prog

# 内存分析
perf stat -e L1-dcache-load-misses,LLC-load-misses ./prog

# 锁分析
perf lock record ./prog && perf lock report
```

### 卡片2: 编译优化组合

```bash
# 通用优化
gcc -O3 -march=native program.c -o program

# 极致性能
gcc -O3 -march=native -flto -fprofile-use \
    -funroll-loops -ffast-math program.c -o program

# 调试性能问题
gcc -O2 -g program.c -o program
```

### 卡片3: 系统调优3板斧

```bash
# 1. CPU性能模式
echo performance | sudo tee /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor

# 2. 启用大页
echo 1024 | sudo tee /proc/sys/vm/nr_hugepages

# 3. 网络优化
sudo sysctl -w net.ipv4.tcp_congestion_control=bbr
```

### 卡片4: 性能问题检查清单

```
□ 使用了合适的算法? (O(n) vs O(n²))
□ 使用了合适的数据结构? (哈希 vs 数组)
□ 启用了编译器优化? (-O2/-O3)
□ 测量了实际瓶颈? (perf/profiler)
□ 数据访问是顺序的? (缓存友好)
□ 避免了不必要的拷贝? (引用/移动)
□ 使用了并行化? (多线程/SIMD)
□ 减少了锁竞争? (无锁/分段)
□ 优化了IO模式? (异步/批量)
□ 验证了优化效果? (对比测试)
```

---

## 9. 资源链接

### 在线工具

- **perf examples**: brendangregg.com/perf.html
- **火焰图**: github.com/brendangregg/FlameGraph
- **编译器优化**: godbolt.org (Compiler Explorer)
- **性能基准**: quick-bench.com

### 文档

- **Intel优化手册**: software.intel.com/optimization
- **AMD优化手册**: developer.amd.com/resources
- **Linux性能**: kernel.org/doc/html/latest/admin-guide/perf
- **GCC优化**: gcc.gnu.org/onlinedocs/gcc/Optimize-Options.html

### 书籍

- **Systems Performance** - Brendan Gregg
- **Computer Architecture** - Hennessy & Patterson
- **The Art of Writing Efficient Programs** - Fedor Pikus

---

## 10. 总结

### 性能优化的核心原则

1. **测量先于优化** - 使用profiler找到瓶颈
2. **大处着眼** - 先优化算法，再优化细节
3. **理解硬件** - 缓存、流水线、SIMD
4. **权衡取舍** - 速度 vs 可读性 vs 维护性
5. **持续验证** - 每次优化都要测量效果

### 优化优先级

```
1. 算法复杂度        (10-1000x)  ← 最高优先级
2. 数据结构选择      (2-10x)
3. 编译器优化        (1.2-3x)
4. 并行化            (2-8x)
5. SIMD向量化        (2-8x)
6. 缓存优化          (1.5-5x)
7. 分支优化          (1.1-2x)
8. 微优化            (1.05-1.2x) ← 最低优先级
```

### 记住

- **80/20法则**: 80%的时间花在20%的代码上
- **Amdahl定律**: 加速比 = 1 / (串行部分 + 并行部分/加速倍数)
- **测量是关键**: 没有测量就没有优化
- **过早优化是万恶之源** - Donald Knuth

---

**速查手册结束**

收藏此文档，在遇到性能问题时快速查找解决方案！

记住：**性能优化是科学，不是艺术。测量、分析、优化、验证。** 🎯
