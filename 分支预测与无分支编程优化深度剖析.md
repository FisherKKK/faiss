# 分支预测与无分支编程优化深度剖析

## 目录
- [1. 概述](#1-概述)
- [2. 分支预测原理](#2-分支预测原理)
- [3. 分支预测失败的代价](#3-分支预测失败的代价)
- [4. 无分支编程技术](#4-无分支编程技术)
- [5. Faiss中的无分支优化](#5-faiss中的无分支优化)
- [6. SIMD无分支技术](#6-simd无分支技术)
- [7. 实战案例](#7-实战案例)
- [8. 性能分析与最佳实践](#8-性能分析与最佳实践)
- [9. 总结](#9-总结)

---

## 1. 概述

### 1.1 什么是分支预测

**分支预测（Branch Prediction）**是CPU为了提高指令级并行性而采用的技术。当遇到条件分支时，CPU需要预测程序的执行路径，提前加载和执行预测路径上的指令。

```cpp
// 带分支的代码
if (condition) {
    do_something();
} else {
    do_something_else();
}
```

**分支预测的作用**：
1. **提高流水线效率**：避免流水线停滞
2. **提高指令级并行**：允许乱序执行
3. **隐藏内存延迟**：提前加载数据

### 1.2 分支预测的重要性

现代CPU的流水线非常深（Intel Skylake: 14-19级），分支预测失败会导致：

1. **清除流水线**：丢弃错误路径上的指令
2. **重新取指**：从正确路径开始取指
3. **性能损失**：约15-20个周期（相当于60-80 ns）

**关键洞察**：在高性能代码中，减少分支预测失败至关重要。

### 1.3 无分支编程

**无分支编程（Branchless Programming）**是一种优化技术，通过使用算术和逻辑运算替代条件分支，从而消除分支预测失败的风险。

```cpp
// 有分支版本
int abs_branch(int x) {
    if (x < 0) {
        return -x;
    }
    return x;
}

// 无分支版本
int abs_branchless(int x) {
    int mask = x >> 31;  // 算术右移：符号位扩展
    return (x + mask) ^ mask;  // 或: (x ^ mask) - mask
}
```

---

## 2. 分支预测原理

### 2.1 CPU流水线

```
取指(F) -> 译码(D) -> 执行(E) -> 访存(M) -> 写回(W)
   ^                                                         |
   |_________________________________________________________|
                            流水线

假设: 每阶段3个周期，5阶段流水线 = 15个周期重叠执行
```

**分支的影响**：
```
正确预测的分支:
F(D0) -> D(D0) -> E(D0) -> M(D0) -> W(D0)
        F(T1) -> D(T1) -> E(T1) -> M(T1) -> W(T1)
                F(T2) -> D(T2) ...

预测失败的分支:
F(D0) -> D(D0) -> E(D0) -> M(D0) -> W(D0)
        F(T1_wrong) -> D(T1_wrong) -> [清除] F(T1_correct) -> D(T1_correct) ...
                                         ^^^^
                                    15-20周期损失
```

### 2.2 分支预测器类型

#### 2.2.1 静态预测

```cpp
// 编译期静态预测（不推荐）
#define LIKELY(x) __builtin_expect((x), 1)
#define UNLIKELY(x) __builtin_expect((x), 0)

if (LIKELY(condition)) {  // 编译器优化：condition很可能为真
    // ...
}

if (UNLIKELY(error_code)) {  // error_code很可能为假
    // 处理错误
}
```

#### 2.2.2 动态预测器

1. **BHT（Branch History Table）**
   - 1位或2位计数器
   - 记录最近的分支方向

2. **两级自适应预测器**
   - 全局历史寄存器（GHR）
   - 模式历史表（PHT）

3. ** Tournament预测器**
   - 组合多个预测器
   - 元预测器选择最优预测器

### 2.3 分支目标缓冲区（BTB）

**BTB（Branch Target Buffer）**缓存分支指令的目标地址，避免取指延迟。

```cpp
// 循环的优化
for (int i = 0; i < n; i++) {  // 分支预测器会学习这个模式
    sum += data[i];
}

// BTB会缓存循环回跳的目标地址
```

---

## 3. 分支预测失败的代价

### 3.1 性能损失分析

```cpp
// 测试代码
void test_branch_prediction() {
    // 有序数据：分支预测准确率高
    std::vector<int> data_ordered = {1, 2, 3, 4, 5, ...};

    // 随机数据：分支预测准确率低
    std::vector<int> data_random = {5, 1, 3, 2, 4, ...};

    // 测试1: 有序数据
    auto start = std::chrono::high_resolution_clock::now();
    long sum = 0;
    for (int i = 0; i < N; i++) {
        if (data_ordered[i] < 50) {  // 高预测准确率
            sum++;
        }
    }
    auto end = std::chrono::high_resolution_clock::now();
    // 时间: ~50ms (预测准确率~95%)

    // 测试2: 随机数据
    start = std::chrono::high_resolution_clock::now();
    sum = 0;
    for (int i = 0; i < N; i++) {
        if (data_random[i] < 50) {  // 低预测准确率
            sum++;
        }
    }
    end = std::chrono::high_resolution_clock::now();
    // 时间: ~500ms (预测准确率~50%)
    // 10倍性能差异！
}
```

### 3.2 为什么随机数据慢

```
分支预测器的学习过程:

有序数据:
1, 2, 3, 4, 5, ... -> 预测: 总是true
T  T  T  T  T      -> 准确率: 100%

随机数据:
5, 1, 3, 2, 4, ... -> 预测: 无法预测
F  T  T  F  T      -> 准确率: ~50%
```

### 3.3 实际影响

| 场景 | 预测准确率 | 相对性能 |
|------|-----------|---------|
| 有序数据 | 95-100% | 1x (基线) |
| 随机数据 | 50% | 10x slower |
| 部分有序 | 70-80% | 3-5x slower |

---

## 4. 无分支编程技术

### 4.1 条件移动（CMOV）

```cpp
// 有分支版本
int max_branch(int a, int b) {
    if (a > b) {
        return a;
    }
    return b;
}

// 无分支版本（使用条件移动）
int max_branchless(int a, int b) {
    int result;
    // 如果a > b，result = a，否则result = b
    // 编译为: CMOV指令
    result = (a > b) ? a : b;
    return result;
}
```

**编译器生成的汇编**：
```asm
; 分支版本
max_branch:
    cmp     edi, esi
    jle     .L2     ; 分支
    mov     eax, edi
    ret
.L2:
    mov     eax, esi
    ret

; 无分支版本
max_branchless:
    cmp     edi, esi
    cmovg   eax, edi   ; 条件移动，无分支
    cmovle  eax, esi
    ret
```

### 4.2 算术选择

```cpp
// 使用算术运算实现选择
int select_branchless(int condition, int a, int b) {
    // condition为0或1
    // 返回 condition ? a : b
    return condition * a + (1 - condition) * b;
}

// 使用位运算（更高效）
int select_bitwise(int condition, int a, int b) {
    // condition为0或-1（所有位为1）
    int mask = -condition;
    return (mask & a) | (~mask & b);
}
```

### 4.3 查找表（LUT）

```cpp
// 使用查找表避免分支
int abs_lut(int x) {
    // 假设x的范围是[-128, 127]
    static const int abs_table[256] = {
        0, 1, 2, 3, ..., 127, 128, 127, ..., 2, 1
    };
    return abs_table[x + 128];
}
```

### 4.4 最小值/最大值（Min/Max）

```cpp
// 有分支版本
int min_branch(int a, int b) {
    return (a < b) ? a : b;  // 编译器通常优化为CMOV
}

// 无分支版本（算术）
int min_branchless(int a, int b) {
    int diff = a - b;
    int mask = (diff >> 31);  // 如果a < b，mask = -1，否则mask = 0
    return (a & mask) | (b & ~mask);
}

// SIMD版本
__m256i min_epi32(__m256i a, __m256i b) {
    return _mm256_min_epi32(a, b);  // 单指令，无分支
}
```

### 4.5 绝对值（Abs）

```cpp
// 有分支版本
int abs_branch(int x) {
    return (x < 0) ? -x : x;
}

// 无分支版本1: 使用掩码
int abs_branchless_1(int x) {
    int mask = x >> 31;
    return (x + mask) ^ mask;
}

// 无分支版本2: 特殊指令
int abs_branchless_2(int x) {
    // x86: ABS指令或特殊算术
    return __builtin_abs(x);  // 编译器会生成最优代码
}

// SIMD版本
__m256i abs_epi32(__m256i x) {
    return _mm256_abs_epi32(x);  // AVX2
}
```

**绝对值算法推导**：
```
如果x >= 0: mask = 0
    (x + 0) ^ 0 = x

如果x < 0: x = -10, mask = -1 (0xFFFFFFFF)
    (x + mask) = -10 + (-1) = -11 = 0xFFFFFFF5
    (-11) ^ (-1) = 0x0000000A = 10  ✓

原理: mask = x >> 31 (算术右移，符号位扩展)
      (x + mask) ^ mask = |x|
```

### 4.6 符号函数（Sign）

```cpp
// 有分支版本
int sign_branch(int x) {
    if (x > 0) return 1;
    if (x < 0) return -1;
    return 0;
}

// 无分支版本
int sign_branchless(int x) {
    return (x > 0) - (x < 0);
}

// 或使用位运算
int sign_branchless_bitwise(int x) {
    return (x >> 31) | (unsigned int)(-x) >> 31;
}
```

---

## 5. Faiss中的无分支优化

### 5.1 堆操作中的无分支技术

```cpp
// faiss/utils/Heap.h

template <class C>
inline void heap_pop(size_t k, typename C::T* bh_val, typename C::TI* bh_ids) {
    bh_val--;
    bh_ids--;

    typename C::T val = bh_val[k];
    typename C::TI id = bh_ids[k];

    size_t i = 1;
    while (true) {
        size_t i2 = i << 1;
        if (i2 > k) {
            break;
        }

        // 无分支的max选择
        // 使用比较和位运算
        i2 += (i2 + 1 <= k) & C::cmp(bh_val[i2 + 1], bh_val[i2]);

        if (!C::cmp(val, bh_val[i2])) {
            break;
        }

        bh_val[i] = bh_val[i2];
        bh_ids[i] = bh_ids[i2];
        i = i2;
    }

    bh_val[i] = val;
    bh_ids[i] = id;
}
```

**无分支max选择分析**：
```cpp
// 传统版本
if (i2 + 1 <= k && bh_val[i2 + 1] > bh_val[i2]) {
    i2 = i2 + 1;
}

// 无分支版本
i2 += (i2 + 1 <= k) & C::cmp(bh_val[i2 + 1], bh_val[i2]);

// 分析:
// (i2 + 1 <= k) 返回 0 或 1
// C::cmp(bh_val[i2 + 1], bh_val[i2]) 返回 0 或 1
// 两者相与得到 0 或 1
// 结果加到i2上
```

### 5.2 SIMD比较中的无分支技术

```cpp
// faiss/impl/simd_result_handlers.h

// 获取小于阈值的元素掩码
uint32_t get_lt_mask(
        uint16_t thr,
        size_t b,
        simd16uint16 d0,
        simd16uint16 d1) {
    simd16uint16 thr16(thr);
    uint32_t lt_mask;

    constexpr bool keep_min = C::is_max;
    if (keep_min) {
        // 使用SIMD比较指令，生成掩码
        lt_mask = ~cmp_ge32(d0, d1, thr16);
    } else {
        lt_mask = ~cmp_le32(d0, d1, thr16);
    }

    if (lt_mask == 0) {
        return 0;
    }

    // 边界检查（无分支）
    uint64_t idx = j0 + b * 32;
    if (idx + 32 > ntotal) {
        if (idx >= ntotal) {
            return 0;
        }
        int nbit = (ntotal - idx);
        lt_mask &= (uint32_t(1) << nbit) - 1;
    }
    return lt_mask;
}
```

### 5.3 位掩码遍历

```cpp
// 使用__builtin_ctz进行无分支遍历
while (lt_mask) {
    // 找到第一个1的位置（无分支）
    int j = __builtin_ctz(lt_mask);

    // 处理元素j
    process_element(j);

    // 清除该位（无分支）
    lt_mask -= 1 << j;
}
```

**为什么这是无分支的**：
- `__builtin_ctz`: 硬件指令（TZCNT或BSF）
- `lt_mask -= 1 << j`: 纯算术运算
- 没有if语句或循环条件跳转（除了while本身）

---

## 6. SIMD无分支技术

### 6.1 SIMD比较指令

```cpp
// SIMD比较返回掩码，不是布尔值
__m256i cmp_gt = _mm256_cmpgt_epi32(a, b);  // a > b?

// 结果不是单个布尔值，而是8个32位掩码
// 每个掩码是全0（false）或全1（true）

// 使用掩码进行选择
__m256i result = _mm256_blendv_epi32(b, a, cmp_gt);
// 如果cmp_gt[i]为全1，选择a[i]，否则选择b[i]
```

### 6.2 SIMD条件选择

```cpp
// SIMD版本的三元运算符
__m256i simd_select(__m256i condition, __m256i a, __m256i b) {
    // condition中的每个元素是0或-1
    // 返回 condition ? a : b
    return _mm256_or_si256(
        _mm256_and_si256(condition, a),
        _mm256_andnot_si256(condition, b)
    );
}

// 或使用blend指令（更高效）
__m256i simd_select_blend(__m256i condition, __m256i a, __m256i b) {
    return _mm256_blendv_epi8(b, a, condition);
}
```

### 6.3 SIMD Min/Max

```cpp
// SIMD Min（单指令，无分支）
__m256i simd_min(__m256i a, __m256i b) {
    return _mm256_min_epi32(a, b);
}

// SIMD Max（单指令，无分支）
__m256i simd_max(__m256i a, __m256i b) {
    return _mm256_max_epi32(a, b);
}

// SIMD绝对值
__m256i simd_abs(__m256i x) {
    return _mm256_abs_epi32(x);
}
```

### 6.4 SIMD计数

```cpp
// 计算SIMD向量中满足条件的元素数量
int count_less_than(__m256i a, __m256i b) {
    // 比较
    __m256i cmp = _mm256_cmpgt_epi32(b, a);

    // 创建掩码
    int mask = _mm256_movemask_epi8(cmp);

    // 计算位1的数量（无分支）
    return __builtin_popcount(mask);
}
```

### 6.5 SIMD查找表

```cpp
// SIMD查找表（AVX2的vpshufb）
__m256i simd_lookup(__m256i indices, const __m256i table[16]) {
    // indices中的每个值是0-15
    // 使用permutevar8x32从table中选择

    // 由于AVX2的限制，需要多次shuffle
    // 这里简化示例

    __m256i result = _mm256_permutevar8x32_epi32(table[0], indices);
    return result;
}
```

---

## 7. 实战案例

### 7.1 案例1: 快速排序中的无分支分区

```cpp
// 有分支的Lomuto分区
int partition_branch(int* arr, int low, int high) {
    int pivot = arr[high];
    int i = low - 1;

    for (int j = low; j < high; j++) {
        if (arr[j] < pivot) {  // 分支
            i++;
            swap(&arr[i], &arr[j]);
        }
    }
    swap(&arr[i + 1], &arr[high]);
    return i + 1;
}

// 无分支的分区（使用条件移动）
int partition_branchless(int* arr, int low, int high) {
    int pivot = arr[high];
    int i = low - 1;

    for (int j = low; j < high; j++) {
        // 使用条件选择而不是分支
        int should_swap = (arr[j] < pivot);
        i += should_swap;
        // 条件交换
        int temp = arr[i];
        arr[i] = should_swap ? arr[j] : arr[i];
        arr[j] = should_swap ? temp : arr[j];
    }
    // 交换pivot
    swap(&arr[i + 1], &arr[high]);
    return i + 1;
}
```

**性能对比**：
- 有序数据：两者性能相似（分支预测准确）
- 随机数据：无分支版本快2-3倍

### 7.2 案例2: 向量点积中的无分支处理

```cpp
// 计算点积，跳过NaN值
float dot_product_skip_nan(const float* a, const float* b, int n) {
    float sum = 0.0f;

    for (int i = 0; i < n; i++) {
        // 有分支版本
        if (!isnan(a[i]) && !isnan(b[i])) {
            sum += a[i] * b[i];
        }
    }
    return sum;
}

// 无分支版本
float dot_product_skip_nan_branchless(const float* a, const float* b, int n) {
    float sum = 0.0f;

    for (int i = 0; i < n; i++) {
        // 创建掩码
        int mask = !isnan(a[i]) && !isnan(b[i]);

        // 使用条件选择
        float contribution = mask ? a[i] * b[i] : 0.0f;
        sum += contribution;
    }
    return sum;
}

// SIMD无分支版本
float dot_product_skip_nan_simd(const float* a, const float* b, int n) {
    __m256 sum = _mm256_setzero_ps();

    for (int i = 0; i < n; i += 8) {
        __m256 va = _mm256_loadu_ps(a + i);
        __m256 vb = _mm256_loadu_ps(b + i);

        // NaN检测（NaN != NaN）
        __m256 is_nan_a = _mm256_cmp_ps(va, va, _CMP_NEQ_UQ);
        __m256 is_nan_b = _mm256_cmp_ps(vb, vb, _CMP_NEQ_UQ);

        // 创建掩码（两者都不是NaN）
        __m256 is_valid = _mm256_andnot_ps(is_nan_a, is_nan_b);

        // 计算乘积
        __m256 product = _mm256_mul_ps(va, vb);

        // 使用掩码选择
        product = _mm256_and_ps(product, is_valid);

        // 累加
        sum = _mm256_add_ps(sum, product);
    }

    // 水平求和
    return horizontal_sum(sum);
}
```

### 7.3 案例3: 直方图统计

```cpp
// 有分支版本
void histogram_branch(const uint8_t* data, int n, int* hist, int bins) {
    memset(hist, 0, bins * sizeof(int));

    for (int i = 0; i < n; i++) {
        int bin = data[i] % bins;

        // 多个分支（如果bins很大，分支预测失败率高）
        if (bin == 0) hist[0]++;
        else if (bin == 1) hist[1]++;
        else if (bin == 2) hist[2]++;
        // ...
    }
}

// 无分支版本
void histogram_branchless(const uint8_t* data, int n, int* hist, int bins) {
    memset(hist, 0, bins * sizeof(int));

    for (int i = 0; i < n; i++) {
        int bin = data[i] % bins;

        // 使用指针算术
        hist[bin] += 1;  // 简单！
    }
}

// SIMD无分支版本（使用Gather指令）
void histogram_simd(const uint8_t* data, int n, int* hist, int bins) {
    memset(hist, 0, bins * sizeof(int));

    for (int i = 0; i + 8 <= n; i += 8) {
        // 加载8个值
        __m256i v = _mm256_loadu_si256((__m256i*)(data + i));

        // SIMD取模（假设bins是2的幂）
        __m256i bins_vec = _mm256_set1_epi32(bins);
        __m256i bin_indices = _mm256_rem_epu16(v, bins_vec);

        // Gather-Scatter操作
        // AVX512: _mm512_i32gather_epi32
        // AVX2: 需要手动实现
    }
}
```

---

## 8. 性能分析与最佳实践

### 8.1 何时使用无分支编程

**适合的场景**：
1. **分支预测困难**：随机或不可预测的条件
2. **热点代码**：执行频率高的代码
3. **SIMD代码**：SIMD难以处理分支
4. **紧凑循环**：循环体小，分支开销大

**不适合的场景**：
1. **分支预测准确**：有序或高度可预测的条件
2. **冷代码**：执行频率低的代码
3. **复杂条件**：无分支代码过于复杂

### 8.2 性能测试

```cpp
#include <benchmark/benchmark.h>

// 有分支版本
static void BM_Branch(benchmark::State& state) {
    std::vector<int> data(state.range(0));
    for (auto _ : state) {
        long sum = 0;
        for (int i = 0; i < state.range(0); i++) {
            if (data[i] < 50) {
                sum++;
            }
        }
        benchmark::DoNotOptimize(sum);
    }
}

// 无分支版本
static void BM_Branchless(benchmark::State& state) {
    std::vector<int> data(state.range(0));
    for (auto _ : state) {
        long sum = 0;
        for (int i = 0; i < state.range(0); i++) {
            sum += (data[i] < 50);
        }
        benchmark::DoNotOptimize(sum);
    }
}

BENCHMARK(BM_Branch)->Range(1024, 1024*1024);
BENCHMARK(BManchless)->Range(1024, 1024*1024);
BENCHMARK_MAIN();
```

### 8.3 编译器优化提示

```cpp
// 使用__builtin_expect提示编译器
if (__builtin_expect(error != 0, 0)) {
    // 错误处理（不太可能执行）
    handle_error(error);
}

// 使用likely/unlikely宏
#define LIKELY(x) __builtin_expect(!!(x), 1)
#define UNLIKELY(x) __builtin_expect(!!(x), 0)

if (LIKELY(ptr != nullptr)) {
    // 主路径
    *ptr = value;
}
```

### 8.4 配合编译器优化

```cpp
// 确保优化级别足够
// g++ -O3 -march=native

// 使用内联函数
inline int min_inline(int a, int b) {
    return a < b ? a : b;  // 编译器会优化为CMOV
}

// 使用constexpr促进编译期计算
constexpr int mask_for_abs(int x) {
    return x >> 31;
}

// 使用模板元编程
template <int N>
struct Factorial {
    static constexpr int value = N * Factorial<N-1>::value;
};
```

### 8.5 调试技巧

```cpp
// 使用性能分析工具
// perf: Linux性能分析工具
$ perf record ./your_program
$ perf report

// 查看分支预测失败率
$ perf stat -e branches,branch-misses ./your_program

// 查看汇编代码
$ objdump -d your_program | less

// 使用Compiler Explorer
// https://godbolt.org/
// 查看编译器生成的汇编代码
```

---

## 9. 总结

### 9.1 关键要点

1. **分支预测失败代价高**
   - 15-20个周期（60-80 ns）
   - 在随机数据上性能下降可达10倍

2. **无分支编程技术**
   - 条件移动（CMOV）
   - 算术选择
   - 查找表（LUT）
   - 位掩码操作

3. **SIMD无分支技术**
   - SIMD比较返回掩码
   - SIMD blend指令
   - SIMD Min/Max/Abs

4. **最佳实践**
   - 分析分支预测准确率
   - 热点代码使用无分支
   - 配合编译器优化
   - 性能测试验证

### 9.2 Faiss中的应用

1. **堆操作**：无分支的max选择
2. **SIMD结果处理**：位掩码遍历
3. **距离计算**：SIMD比较和选择
4. **直方图**：SIMD histogram实现

### 9.3 权衡考虑

| 方面 | 有分支 | 无分支 |
|------|--------|--------|
| 代码可读性 | 高 | 中 |
| 可维护性 | 高 | 中 |
| 性能（可预测） | 高 | 中 |
| 性能（不可预测） | 低 | 高 |
| SIMD友好度 | 低 | 高 |

### 9.4 进一步学习

1. **编译器优化**
   - SSA形式
   - 寄存器分配
   - 指令调度

2. **CPU架构**
   - 分支预测器设计
   - 流水线结构
   - 乱序执行

3. **SIMD编程**
   - AVX/AVX2/AVX-512
   - ARM NEON
   - 向量化算法设计

---

## 附录A：常用无分支模式

```cpp
// 1. 绝对值
int abs(int x) {
    int mask = x >> 31;
    return (x + mask) ^ mask;
}

// 2. 最小值
int min(int a, int b) {
    int diff = a - b;
    int mask = diff >> 31;
    return b & mask | a & ~mask;
}

// 3. 最大值
int max(int a, int b) {
    int diff = a - b;
    int mask = diff >> 31;
    return a & mask | b & ~mask;
}

// 4. 符号函数
int sign(int x) {
    return (x > 0) - (x < 0);
}

// 5. 条件选择
int select(int condition, int a, int b) {
    return (condition & a) | (~condition & b);
}

// 6. 幂运算（2的幂）
int pow2(int n) {
    return 1 << n;
}

// 7. 对数（以2为底）
int log2(int x) {
    return 31 - __builtin_clz(x);
}

// 8. 位计数
int popcount(int x) {
    return __builtin_popcount(x);
}
```

## 附录B：SIMD无分支指令

| 指令 | 描述 | 延迟 | 吞吐量 |
|------|------|------|--------|
| `pcmpeq` | SIMD相等比较 | 1 | 0.5 |
| `pcmpgt` | SIMD大于比较 | 1 | 0.5 |
| `pmin/pmax` | SIMD最小/最大 | 1 | 1 |
| `pblend` | SIMD条件选择 | 1 | 1 |
| `cmov` | 标量条件移动 | 1-2 | 1-2 |
| `select` | ARM条件选择 | 1 | 1 |

## 附录C：性能分析工具

```bash
# Linux perf
perf stat -e branches,branch-misses,cycles ./program
perf record -e branch-misses ./program
perf report

# VTune (Intel)
vtune -collect brnach-events ./program

# perf annotate
perf record ./program
perf annotate

# 查看汇编
objdump -d -M intel program | less
```

## 附录D：相关源文件

- `faiss/utils/Heap.h` - 堆操作中的无分支技术
- `faiss/impl/simd_result_handlers.h` - SIMD结果处理
- `faiss/utils/simdlib.h` - SIMD库定义
- `faiss/utils/distances_simd.cpp` - SIMD距离计算
