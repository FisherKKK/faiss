# AVX-512高级优化技巧深度剖析

## 课程简介

本课程深入剖析AVX-512指令集的高级优化技巧,涵盖掩码操作、融合指令、gather/scatter等高级特性,以及如何在实际场景中充分发挥AVX-512的性能潜力。

**前置知识**:
- 已完成《SIMD深度实践课程-AVX512与ARMNEON》
- 已完成《SIMD底层优化深度剖析》
- 熟悉x86汇编语言和CPU微架构

**学习目标**:
- 掌握AVX-512的高级指令特性
- 理解掩码操作在向量化中的应用
- 学习gather/scatter等非连续内存访问优化
- 掌握AVX-512特有的融合指令和性能优化

---

## 第一部分:AVX-512掩码操作深度剖析

### 1.1 掩码的类型和用途

AVX-512提供了三种主要的掩码操作:

```cpp
// 1. 比较掩码 (Comparison Masks)
__mmask16 cmp_mask = _mm512_cmp_ps_mask(a, b, _CMP_GT_OQ);

// 2. 控制掩码 (Operation Masks)
__m512 result = _mm512_add_ps(a, b, mask);  // 只在mask=1的位置计算

// 3. 合并掩码 (Blend Masks)
__m512 blended = _mm512_mask_blend_ps(mask, a, b);  // 掩码选择
```

### 1.2 条件执行的优化

```cpp
// 传统方式: 使用分支
void conditional_add_scalar(
        const float* a,
        const float* b,
        float* c,
        const float* threshold,
        size_t n) {

    for (size_t i = 0; i < n; i++) {
        if (a[i] > threshold[i]) {
            c[i] = a[i] + b[i];
        } else {
            c[i] = a[i];
        }
    }
}

// AVX-512优化: 无分支版本
void conditional_add_avx512(
        const float* a,
        const float* b,
        float* c,
        const float* threshold,
        size_t n) {

    size_t i = 0;

    // 处理16的倍数
    for (; i + 16 <= n; i += 16) {
        __m512 va = _mm512_loadu_ps(a + i);
        __m512 vb = _mm512_loadu_ps(b + i);
        __m512 vt = _mm512_loadu_ps(threshold + i);

        // 生成掩码: a > threshold
        __mmask16 mask = _mm512_cmp_ps_mask(va, vt, _CMP_GT_OQ);

        // 掩码加法: 只在mask=1的位置执行加法
        __m512 vsum = _mm512_mask_add_ps(va, mask, va, vb);

        _mm512_storeu_ps(c + i, vsum);
    }

    // 处理剩余元素
    for (; i < n; i++) {
        c[i] = (a[i] > threshold[i]) ? (a[i] + b[i]) : a[i];
    }
}
```

### 1.3 掩码压缩与展开

```cpp
// 压缩: 根据掩码提取有效元素
void compress_example(const float* input, float* output, size_t n) {
    size_t i = 0;
    size_t write_idx = 0;

    for (; i + 16 <= n; i += 16) {
        __m512 vin = _mm512_loadu_ps(input + i);
        __m512 vthreshold = _mm512_set1_ps(0.0f);

        // 生成掩码: input > 0
        __mmask16 mask = _mm512_cmp_ps_mask(vin, vthreshold, _CMP_GT_OQ);

        if (mask != 0) {
            // 计算有效元素数量
            int count = _mm_popcnt_u32(mask);

            // 压缩有效元素到output
            __m512 vout = _mm512_maskz_compress_ps(mask, vin);

            // 存储到output (注意:压缩后元素在低位置)
            if (write_idx + 16 <= n) {
                _mm512_storeu_ps(output + write_idx, vout);
            } else {
                // 使用掩码存储(处理边界)
                __mmask16 store_mask = (0xFFFF >> (16 - count));
                _mm512_mask_storeu_ps(output + write_idx, store_mask, vout);
            }

            write_idx += count;
        }
    }
}

// 展开: 根据掩码分散元素
void expand_example(const float* input, const uint8_t* mask, float* output, size_t n) {
    size_t i = 0;

    for (; i + 16 <= n; i += 16) {
        __m512 vin = _mm512_loadu_ps(input + i);

        // 从mask生成__mmask16
        __mmask16 vmask = _mm512_cmpneq_epu8_mask(
            _mm512_loadu_si512((__m512i*)(mask + i)),
            _mm512_setzero_si512()
        );

        // 展开到output
        __m512 vout = _mm512_mask_expand_ps(_mm512_setzero_ps(), vmask, vin);
        _mm512_storeu_ps(output + i, vout);
    }
}
```

### 1.4 掩码归约操作

```cpp
// 带掩码的水平归约
float masked_horizontal_sum(const float* data, const uint8_t* valid, size_t n) {
    __m512 vsum = _mm512_setzero_ps();
    size_t i = 0;

    for (; i + 16 <= n; i += 16) {
        __m512 vdata = _mm512_loadu_ps(data + i);

        // 生成有效掩码
        __mmask16 vmask = _mm512_cmpneq_epu8_mask(
            _mm512_loadu_si512((__m512i*)(valid + i)),
            _mm512_setzero_si512()
        );

        // 掩码加法
        vsum = _mm512_mask_add_ps(vsum, vmask, vsum, vdata);
    }

    return _mm512_reduce_add_ps(vsum);
}

// 带掩码的最大值/最小值
void masked_minmax(
        const float* data,
        const uint8_t* valid,
        size_t n,
        float& min_val,
        float& max_val) {

    __m512 vmin = _mm512_set1_ps(FLT_MAX);
    __m512 vmax = _mm512_set1_ps(-FLT_MAX);
    size_t i = 0;

    for (; i + 16 <= n; i += 16) {
        __m512 vdata = _mm512_loadu_ps(data + i);

        __mmask16 vmask = _mm512_cmpneq_epu8_mask(
            _mm512_loadu_si512((__m512i*)(valid + i)),
            _mm512_setzero_si512()
        );

        vmin = _mm512_mask_min_ps(vmin, vmask, vmin, vdata);
        vmax = _mm512_mask_max_ps(vmax, vmask, vmax, vdata);
    }

    min_val = _mm512_reduce_min_ps(vmin);
    max_val = _mm512_reduce_max_ps(vmax);
}
```

---

## 第二部分:AVX-512融合指令

### 2.1 FMA变体

AVX-512提供了多种FMA指令形式:

```cpp
// 基础FMA: a * b + c
__m512 fmadd_1 = _mm512_fmadd_ps(a, b, c);     // +c
__m512 fmadd_2 = _mm512_fmsub_ps(a, b, c);     // -c
__m512 fmadd_3 = _mm512_fnmadd_ps(a, b, c);    // -(a*b) + c
__m512 fmadd_4 = _mm512_fnmsub_ps(a, b, c);    // -(a*b) - c

// 实际应用: L2距离计算优化
float l2_distance_fma(const float* x, const float* y, size_t d) {
    __m512 sum = _mm512_setzero_ps();
    size_t i = 0;

    for (; i + 16 <= d; i += 16) {
        __m512 vx = _mm512_loadu_ps(x + i);
        __m512 vy = _mm512_loadu_ps(y + i);

        __m512 diff = _mm512_sub_ps(vx, vy);

        // 使用FMA: sum = diff * diff + sum
        sum = _mm512_fmadd_ps(diff, diff, sum);
    }

    return _mm512_reduce_add_ps(sum);
}
```

### 2.2 点积优化

```cpp
// 点积的多种实现方式

// 方式1: 传统FMA
float dot_product_1(const float* x, const float* y, size_t d) {
    __m512 sum = _mm512_setzero_ps();
    size_t i = 0;

    for (; i + 16 <= d; i += 16) {
        __m512 vx = _mm512_loadu_ps(x + i);
        __m512 vy = _mm512_loadu_ps(y + i);

        sum = _mm512_fmadd_ps(vx, vy, sum);
    }

    return _mm512_reduce_add_ps(sum);
}

// 方式2: 使用dp指令 (如果支持)
// 注意: 不是所有CPU都支持此指令
float dot_product_2(const float* x, const float* y, size_t d) {
    // 检查CPU是否支持AVX512F/AVX512DQ等
    // ...
}

// 方式3: 手动展开优化
float dot_product_3(const float* x, const float* y, size_t d) {
    __m512 sum0 = _mm512_setzero_ps();
    __m512 sum1 = _mm512_setzero_ps();
    size_t i = 0;

    // 4路展开
    for (; i + 64 <= d; i += 64) {
        __m512 vx0 = _mm512_loadu_ps(x + i);
        __m512 vy0 = _mm512_loadu_ps(y + i);
        __m512 vx1 = _mm512_loadu_ps(x + i + 16);
        __m512 vy1 = _mm512_loadu_ps(y + i + 16);
        __m512 vx2 = _mm512_loadu_ps(x + i + 32);
        __m512 vy2 = _mm512_loadu_ps(y + i + 32);
        __m512 vx3 = _mm512_loadu_ps(x + i + 48);
        __m512 vy3 = _mm512_loadu_ps(y + i + 48);

        sum0 = _mm512_fmadd_ps(vx0, vy0, sum0);
        sum1 = _mm512_fmadd_ps(vx1, vy1, sum1);
        sum0 = _mm512_fmadd_ps(vx2, vy2, sum0);
        sum1 = _mm512_fmadd_ps(vx3, vy3, sum1);
    }

    sum0 = _mm512_add_ps(sum0, sum1);
    return _mm512_reduce_add_ps(sum0);
}
```

---

## 第三部分:Gather与Scatter操作

### 3.1 Gather: 非连续内存加载

```cpp
// 使用gather进行非连续内存访问
void gather_example(
        const float* base,
        const int* indices,
        float* output,
        size_t n) {

    size_t i = 0;

    // 每次加载16个非连续元素
    for (; i + 16 <= n; i += 16) {
        // 加载索引
        __m512i vindices = _mm512_loadu_si512((__m512i*)(indices + i));

        // 根据索引gather数据
        __m512 vdata = _mm512_i32gather_ps(vindices, base, 4);  // scale=4

        _mm512_storeu_ps(output + i, vdata);
    }

    // 处理剩余元素
    for (; i < n; i++) {
        output[i] = base[indices[i]];
    }
}

// 实际应用: 查表操作
void lookup_table_avx512(
        const uint8_t* indices,  // 输入索引
        const float* table,      // 查找表: 256个float
        float* output,
        size_t n) {

    size_t i = 0;

    for (; i + 16 <= n; i += 16) {
        // 加载16个索引
        __m128i vidx128 = _mm_loadu_si128((__m128i*)(indices + i));

        // 扩展为32位整数
        __m256i vidx256_lo = _mm256_cvtepu8_epi32(vidx128);
        __m256i vidx256_hi = _mm256_cvtepu8_epi32(
            _mm_loadu_si128((__m128i*)(indices + i + 8))
        );

        // 合并为512位
        __m512i vidx = _mm512_castsi256_si512(vidx256_lo);
        vidx = _mm512_inserti32x4(vidx, vidx256_hi, 1);

        // Gather查找表数据
        __m512 vresult = _mm512_i32gather_ps(vidx, table, 4);

        _mm512_storeu_ps(output + i, vresult);
    }
}
```

### 3.2 Scatter: 非连续内存存储

```cpp
// 使用scatter进行非连续内存写入
void scatter_example(
        const float* data,
        int* indices,
        float* base,
        size_t n) {

    size_t i = 0;

    for (; i + 16 <= n; i += 16) {
        __m512 vdata = _mm512_loadu_ps(data + i);
        __m512i vindices = _mm512_loadu_si512((__m512i*)(indices + i));

        // Scatter数据到非连续位置
        _mm512_i32scatter_ps(base, vindices, vdata, 4);
    }

    for (; i < n; i++) {
        base[indices[i]] = data[i];
    }
}

// 应用: 稀疏矩阵-向量乘法
void sparse_matvec_scatter(
        const float* values,
        const int* col_indices,
        const int* row_ptr,
        const float* x,
        float* y,
        size_t n_rows) {

    for (size_t i = 0; i < n_rows; i++) {
        float sum = 0.0f;
        size_t row_start = row_ptr[i];
        size_t row_end = row_ptr[i + 1];

        size_t j = row_start;

        // 使用scatter-gather优化
        for (; j + 16 <= row_end; j += 16) {
            __m512 vvalues = _mm512_loadu_ps(values + j);
            __m512i vindices = _mm512_loadu_si512(
                (__m512i*)(col_indices + j)
            );

            // Gather x元素
            __m512 vx = _mm512_i32gather_ps(vindices, x, 4);

            // 累加
            sum = _mm512_reduce_add_ps(_mm512_mul_ps(vvalues, vx));
        }

        // 处理剩余元素
        for (; j < row_end; j++) {
            sum += values[j] * x[col_indices[j]];
        }

        y[i] = sum;
    }
}
```

### 3.3 带掩码的Gather/Scatter

```cpp
// 带掩码的gather
void masked_gather(
        const float* base,
        const int* indices,
        const uint8_t* valid,
        float* output,
        size_t n) {

    size_t i = 0;

    for (; i + 16 <= n; i += 16) {
        __m512i vindices = _mm512_loadu_si512((__m512i*)(indices + i));

        // 生成掩码
        __mmask16 vmask = _mm512_cmpneq_epu8_mask(
            _mm512_loadu_si512((__m512i*)(valid + i)),
            _mm512_setzero_si512()
        );

        // 掩码gather
        __m512 vdata = _mm512_mask_i32gather_ps(
            _mm512_setzero_ps(),  // 初始值
            vmask,
            vindices,
            base,
            4
        );

        _mm512_storeu_ps(output + i, vdata);
    }
}

// 带掩码的scatter
void masked_scatter(
        const float* data,
        int* indices,
        const uint8_t* valid,
        float* base,
        size_t n) {

    size_t i = 0;

    for (; i + 16 <= n; i += 16) {
        __m512 vdata = _mm512_loadu_ps(data + i);
        __m512i vindices = _mm512_loadu_si512((__m512i*)(indices + i));

        __mmask16 vmask = _mm512_cmpneq_epu8_mask(
            _mm512_loadu_si512((__m512i*)(valid + i)),
            _mm512_setzero_si512()
        );

        // 掩码scatter
        _mm512_mask_i32scatter_ps(base, vmask, vindices, vdata, 4);
    }
}
```

---

## 第四部分:高级排列和转换操作

### 4.1 Permute操作

```cpp
// 跨512位寄存器的排列
void permute_example(const float* input, float* output) {
    __m512 v0 = _mm512_loadu_ps(input);
    __m512 v1 = _mm512_loadu_ps(input + 16);

    // permute: 重排128位块
    // 控制字: [src3[127:0], src2[127:0], src1[127:0], src0[127:0]]
    __m512 v0_perm = _mm512_permutex_ps(v0, _MM_SHUFFLE(3, 2, 1, 0));  // 反转
    __m512 v1_perm = _mm512_permutex_ps(v1, _MM_SHUFFLE(0, 1, 2, 3));

    _mm512_storeu_ps(output, v0_perm);
    _mm512_storeu_ps(output + 16, v1_perm);
}

// shufflevar: 可变shuffle
void shufflevar_example(const float* input, const uint8_t* indices, float* output) {
    __m512 vdata = _mm512_loadu_ps(input);
    __m512i vindices = _mm512_loadu_si512((__m512i*)indices);

    // 使用索引进行shuffle
    __m512 vshuffled = _mm512_permutexvar_ps(vindices, vdata);

    _mm512_storeu_ps(output, vshuffled);
}
```

### 4.2 类型转换

```cpp
// 各种类型转换操作
void conversion_examples() {
    // float32 <-> int32
    __m512 vf = _mm512_set1_ps(3.14f);
    __m512i vi = _mm512_cvtps_epi32(vf);  // float -> int
    __m512 vf2 = _mm512_cvtepi32_ps(vi);  // int -> float

    // uint8 -> int32 (扩展)
    __m128i v8 = _mm_set1_epi8(42);
    __m256i v32_lo = _mm256_cvtepu8_epi32(v8);
    __m256i v32_hi = _mm256_cvtepu8_epi32(_mm_srli_si128(v8, 1));

    // int32 -> float (有符号扩展)
    __m512i vi_signed = _mm512_set1_epi32(-100);
    __m512 vf_signed = _mm512_cvtepi32_ps(vi_signed);

    // int32 -> float (无符号扩展)
    __m512i vi_unsigned = _mm512_set1_epi32(100);
    __m512 vf_unsigned = _mm512_cvtepu32_ps(vi_unsigned);
}
```

### 4.3 位操作优化

```cpp
// 位操作用于快速过滤
void bit_operations_example() {
    // 提取符号位
    __m512 vf = _mm512_loadu_ps(some_data);
    __mmask16 sign_mask = _mm512_move_mm_ps_mask(vf, 0xF);

    // 位计数
    __m512i vi = _mm512_loadu_si512((__m512i*)some_int_data);
    int popcount = _mm512_popcnt_epi64(vi);  // 统计1的个数

    // 前导零计数
    int lz_count = _mm512_lzcnt_epi32(vi);

    // 位反转
    __m512i vrev = _mm512_bitshuffle_epi64_mask(vi, vi);
}

// 使用位操作实现快速查找
int find_first_set_avx512(const uint64_t* data, size_t n) {
    size_t i = 0;

    for (; i + 8 <= n; i += 8) {
        __m512i vdata = _mm512_loadu_si512((__m512i*)(data + i));

        // 测试是否为0
        __mmask16 mask = _mm512_test_epi64_mask(vdata, vdata);

        if (mask != 0) {
            // 找到第一个非0元素
            int index = _mm_tzcnt_64(mask);
            return i * 64 + index;
        }
    }

    return -1;
}
```

---

## 第五部分:AVX-512性能优化

### 5.1 避免端口竞争

```cpp
// AVX-512有不同的执行端口
// p0/p5: ALU, p1: 乘法, p2: load, p3: store, etc.

// 优化前: 都依赖p5 (ALU)
__m512 bad_code(__m512 a, __m512 b, __m512 c) {
    __m512 r1 = _mm512_add_ps(a, b);     // p5
    __m512 r2 = _mm512_add_ps(r1, c);    // p5, 依赖r1
    __m512 r3 = _mm512_add_ps(r2, a);    // p5, 依赖r2
    return r3;
}

// 优化后: 分散到不同端口
__m512 good_code(__m512 a, __m512 b, __m512 c) {
    // 使用FMA: 可以在p1(乘法)执行
    __m512 r1 = _mm512_fmadd_ps(a, b, c);  // p1
    __m512 r2 = _mm512_fmadd_ps(a, b, r1);  // p1
    __m512 r3 = _mm512_fmadd_ps(a, b, r2);  // p1
    return r3;
}
```

### 5.2 向量长度优化

```cpp
// 根据向量长度选择最优实现
void dot_product_adaptive(
        const float* x,
        const float* y,
        size_t d,
        float* result) {

    float sum = 0.0f;
    size_t i = 0;

    // 512位: 16个float
    if (d >= 16) {
        __m512 vsum = _mm512_setzero_ps();

        for (; i + 16 <= d; i += 16) {
            __m512 vx = _mm512_loadu_ps(x + i);
            __m512 vy = _mm512_loadu_ps(y + i);
            vsum = _mm512_fmadd_ps(vx, vy, vsum);
        }

        sum = _mm512_reduce_add_ps(vsum);
    }

    // 256位: 8个float
    if (i + 8 <= d) {
        __m256 vsum = _mm256_setzero_ps();

        for (; i + 8 <= d; i += 8) {
            __m256 vx = _mm256_loadu_ps(x + i);
            __m256 vy = _mm256_loadu_ps(y + i);
            vsum = _mm256_fmadd_ps(vx, vy, vsum);
        }

        sum += _mm256_reduce_add_ps(vsum);
    }

    // 标量: 处理剩余
    for (; i < d; i++) {
        sum += x[i] * y[i];
    }

    *result = sum;
}
```

### 5.3 预取优化

```cpp
// AVX-512预取指令
void prefetch_optimized(
        const float* x,
        const float* y,
        float* result,
        size_t n) {

    const size_t prefetch_distance = 8;  // 预取8个向量

    size_t i = 0;

    for (; i + 16 <= n; i += 16) {
        // 预取未来数据
        if (i + prefetch_distance * 16 < n) {
            _mm_prefetch((char*)(x + i + prefetch_distance * 16), _MM_HINT_T0);
            _mm_prefetch((char*)(y + i + prefetch_distance * 16), _MM_HINT_T0);
        }

        __m512 vx = _mm512_loadu_ps(x + i);
        __m512 vy = _mm512_loadu_ps(y + i);
        __m512 vr = _mm512_mul_ps(vx, vy);
        _mm512_storeu_ps(result + i, vr);
    }
}
```

### 5.4 缓存对齐优化

```cpp
// 分配对齐的内存
float* allocate_aligned_avx512(size_t n) {
    void* ptr = nullptr;
    // 64字节对齐 (AVX-512缓存行)
    int ret = posix_memalign(&ptr, 64, n * sizeof(float));

    if (ret != 0) {
        throw std::bad_alloc();
    }

    return static_cast<float*>(ptr);
}

// 使用对齐加载(更快)
void aligned_load_example() {
    // 分配对齐的内存
    float* a = allocate_aligned_avx512(32);
    float* b = allocate_aligned_avx512(32);
    float* c = allocate_aligned_avx512(32);

    // 使用对齐加载 (要求64字节对齐)
    __m512 va = _mm512_load_ps(a);  // 对齐,快
    __m512 vb = _mm512_load_ps(b);
    __m512 vc = _mm512_add_ps(va, vb);

    // 对齐存储
    _mm512_store_ps(c, vc);

    free(a);
    free(b);
    free(c);
}
```

---

## 第六部分:实际应用案例

### 6.1 矩阵乘法优化

```cpp
// AVX-512矩阵乘法 (16x16)
void matmul_16x16_avx512(
        const float* A,  // 16x16
        const float* B,  // 16x16
        float* C) {      // 16x16

    // 清零C
    memset(C, 0, 16 * 16 * sizeof(float));

    for (int i = 0; i < 16; i++) {
        for (int j = 0; j < 16; j++) {
            __m512 vc = _mm512_setzero_ps();

            for (int k = 0; k < 16; k++) {
                // 加载A的第i行
                float a_ik = A[i * 16 + k];
                __m512 va = _mm512_set1_ps(a_ik);

                // 加载B的第j列
                __m512 vb = _mm512_loadu_ps(B + k * 16 + j * 16);

                // 累加
                vc = _mm512_fmadd_ps(va, vb, vc);
            }

            // 存储到C
            _mm512_storeu_ps(C + i * 16 + j * 16, vc);
        }
    }
}

// 更优化的版本: 分块矩阵乘法
void matmul_blocked_avx512(
        const float* A,
        const float* B,
        float* C,
        size_t M, size_t N, size_t K) {

    const size_t BLOCK = 16;  // AVX-512宽度

    for (size_t i = 0; i < M; i += BLOCK) {
        for (size_t j = 0; j < N; j += BLOCK) {
            // 清零C块
            for (size_t ii = 0; ii < BLOCK && i + ii < M; ii++) {
                for (size_t jj = 0; jj < BLOCK && j + jj < N; jj++) {
                    C[(i + ii) * N + (j + jj)] = 0.0f;
                }
            }

            // 累加K块
            for (size_t k = 0; k < K; k += BLOCK) {
                for (size_t ii = 0; ii < BLOCK && i + ii < M; ii++) {
                    for (size_t kk = 0; kk < BLOCK && k + kk < K; kk++) {
                        float a_ik = A[(i + ii) * K + (k + kk)];

                        for (size_t jj = 0; jj < BLOCK && j + jj < N; jj++) {
                            float b_kj = B[(k + kk) * N + (j + jj)];
                            C[(i + ii) * N + (j + jj)] += a_ik * b_kj;
                        }
                    }
                }
            }
        }
    }
}
```

### 6.2 卷积优化

```cpp
// AVX-512优化的1D卷积
void conv1d_avx512(
        const float* input,
        const float* kernel,
        float* output,
        size_t input_size,
        size_t kernel_size) {

    size_t output_size = input_size - kernel_size + 1;
    size_t i = 0;

    // 主循环: 每次计算16个输出
    for (; i + 16 <= output_size; i += 16) {
        __m512 vsum = _mm512_setzero_ps();

        // 遍历kernel
        for (size_t k = 0; k < kernel_size; k++) {
            float k_val = kernel[k];

            // 加载16个输入
            __m512 vinput = _mm512_loadu_ps(input + i + k);

            // 累加
            vsum = _mm512_fmadd_ps(_mm512_set1_ps(k_val), vinput, vsum);
        }

        _mm512_storeu_ps(output + i, vsum);
    }

    // 处理剩余输出
    for (; i < output_size; i++) {
        float sum = 0.0f;
        for (size_t k = 0; k < kernel_size; k++) {
            sum += input[i + k] * kernel[k];
        }
        output[i] = sum;
    }
}
```

### 6.3 排序优化

```cpp
// Bitonic排序 (AVX-512版本)
void bitonic_sort_avx512(float* data, size_t n) {
    // Bitonic排序需要n是2的幂次
    assert((n & (n - 1)) == 0);

    for (size_t k = 2; k <= n; k *= 2) {
        for (size_t j = k / 2; j > 0; j /= 2) {
            for (size_t i = 0; i < n; i++) {
                size_t l = i ^ j;

                if (l > i) {
                    if ((i & k) == 0 && data[i] > data[l]) {
                        std::swap(data[i], data[l]);
                    } else if ((i & k) != 0 && data[i] < data[l]) {
                        std::swap(data[i], data[l]);
                    }
                }
            }
        }
    }
}

// SIMD优化的部分排序 (前16个元素)
void partial_sort_16_avx512(float* data, size_t n) {
    if (n <= 16) {
        // 对所有元素排序
        std::sort(data, data + n);
        return;
    }

    // 使用AVX-512找到前16个最小元素
    __m512 vmin0 = _mm512_set1_ps(FLT_MAX);
    __m512 vmin1 = _mm512_set1_ps(FLT_MAX);

    for (size_t i = 0; i < n; i += 16) {
        __m512 vdata = _mm512_loadu_ps(data + i);

        // 更新最小值
        __mmask16 cmp0 = _mm512_cmplt_ps_mask(vdata, vmin0);
        __mmask16 cmp1 = _mm512_cmplt_ps_mask(vdata, vmin1);

        vmin0 = _mm512_mask_min_ps(vmin0, cmp0, vmin0, vdata);
        vmin1 = _mm512_mask_min_ps(vmin1, cmp1, vmin1, vdata);
    }

    // ... (实现完整的前16排序)
}
```

---

## 第七部分:性能分析

### 7.1 指令延迟和吞吐量

| 指令 | 延迟 | 吞吐量 | 端口 |
|------|------|--------|------|
| add_ps | 4 | 0.5 | p5 |
| mul_ps | 4 | 0.5 | p5 |
| fmadd_ps | 4 | 0.5 | p5 |
| load_ps | ~10 | 0.5 | p2 |
| store_ps | ~10 | 1 | p3 |
| gather_ps | ~15 | 2 | p2/p5 |
| permute | 3 | 1 | p5 |

### 7.2 性能测量

```cpp
#include <chrono>
#include <immintrin.h>
#include <cpuid.h>

// 检测AVX-512支持
bool check_avx512_support() {
    unsigned int eax, ebx, ecx, edx;

    // 检查AVX512F
    __cpuid_count(7, 0, eax, ebx, ecx, edx);
    bool has_avx512f = (ebx & (1 << 16)) != 0;

    // 检查AVX512DQ
    bool has_avx512dq = (ebx & (1 << 17)) != 0;

    // 检查AVX512VL
    bool has_avx512vl = (ecx & (1 << 31)) != 0;  // 注意: ECX bit 31

    printf("AVX-512F: %s\n", has_avx512f ? "YES" : "NO");
    printf("AVX-512DQ: %s\n", has_avx512dq ? "YES" : "NO");
    printf("AVX-512VL: %s\n", has_avx512vl ? "YES" : "NO");

    return has_avx512f;
}

// 性能基准测试
void benchmark_dot_product() {
    const size_t d = 1024;
    const size_t iterations = 1000000;

    float* x = new float[d];
    float* y = new float[d];

    // 初始化
    for (size_t i = 0; i < d; i++) {
        x[i] = (float)i / d;
        y[i] = (float)(d - i) / d;
    }

    // 测试AVX-512版本
    auto start = std::chrono::high_resolution_clock::now();

    float sum = 0.0f;
    for (size_t iter = 0; iter < iterations; iter++) {
        sum += dot_product_1(x, y, d);  // AVX-512版本
    }

    auto end = std::chrono::high_resolution_clock::now();
    double elapsed = std::chrono::duration<double>(end - start).count();

    printf("AVX-512: %.3f us/iter, result=%.3f\n",
           elapsed / iterations * 1e6, sum);

    delete[] x;
    delete[] y;
}
```

### 7.3 编译器优化建议

```bash
# 启用AVX-512的编译选项
gcc -march=skylake-avx512 -O3 -ffast-math code.cpp

# 或针对特定CPU
gcc -march=cascadelake -O3 code.cpp
gcc -march=icelake -O3 code.cpp

# 检查生成的汇编
gcc -S -masm=intel -march=skylake-avx512 code.cpp

# 查看自动向量化报告
gcc -O3 -fopt-info-vec-optimized code.cpp
```

---

## 第八部分:常见陷阱与注意事项

### 8.1 频率降频问题

```cpp
// AVX-512可能导致CPU降频
// 许多Intel CPU在使用AVX-512时降低频率

// 解决方案1: 混合使用SIMD宽度
void hybrid_simd_code(const float* a, const float* b, float* c, size_t n) {
    size_t i = 0;

    // 使用AVX-512处理主循环
    for (; i + 16 <= n; i += 16) {
        __m512 va = _mm512_loadu_ps(a + i);
        __m512 vb = _mm512_loadu_ps(b + i);
        __m512 vc = _mm512_mul_ps(va, vb);
        _mm512_storeu_ps(c + i, vc);
    }

    // 使用AVX2处理中等块
    for (; i + 8 <= n; i += 8) {
        __m256 va = _mm256_loadu_ps(a + i);
        __m256 vb = _mm256_loadu_ps(b + i);
        __m256 vc = _mm256_mul_ps(va, vb);
        _mm256_storeu_ps(c + i, vc);
    }

    // 标量处理剩余
    for (; i < n; i++) {
        c[i] = a[i] * b[i];
    }
}
```

### 8.2 对齐问题

```cpp
// 错误: 未对齐访问
void wrong_aligned_access(float* ptr) {
    // 如果ptr不是64字节对齐,可能会崩溃
    __m512 v = _mm512_load_ps(ptr);  // 危险!
}

// 正确: 使用对齐加载或检查对齐
void correct_aligned_access(float* ptr) {
    // 方式1: 使用非对齐加载
    __m512 v = _mm512_loadu_ps(ptr);  // 安全

    // 方式2: 检查对齐
    if ((uintptr_t)ptr % 64 == 0) {
        __m512 v = _mm512_load_ps(ptr);  // 安全
    } else {
        __m512 v = _mm512_loadu_ps(ptr);
    }
}
```

### 8.3 混合精度计算

```cpp
// BF16 (BFloat16) 支持
void bf16_computations() {
    #ifdef __AVX512BF16__
    // BF16类型
    __m512bh vbf = _mm512_set1_ph(3.14);  // BF16

    // FP32 <-> BF16转换
    __m512 vf = _mm512_castbh_ps512(vbf);  // BF16 -> FP32
    __m512bh vbf2 = _mm512_cvtneps_pbh(vf); // FP32 -> BF16

    // BF16计算 (内部转换为FP32)
    __m512bh vbf_a = _mm512_loadu_ph(bf16_data);
    __m512bh vbf_b = _mm512_loadu_ph(bf16_data + 16);

    __m512 vf_a = _mm512_castbh_ps512(vbf_a);
    __m512 vf_b = _mm512_castbh_ps512(vbf_b);
    __m512 vf_c = _mm512_add_ps(vf_a, vf_b);
    #endif
}
```

---

## 总结

本课程深入剖析了AVX-512的高级优化技术:

1. **掩码操作**: 条件执行、压缩展开、掩码归约
2. **融合指令**: FMA变体、点积优化
3. **Gather/Scatter**: 非连续内存访问优化
4. **高级排列**: Permute、shufflevar、类型转换
5. **性能优化**: 端口竞争、向量长度、预取
6. **实际应用**: 矩阵乘法、卷积、排序
7. **注意事项**: 频率降频、对齐问题、混合精度

**关键要点**:
- AVX-512的掩码操作是实现无分支编程的强大工具
- Gather/Scatter可以优化非连续访问,但延迟较高
- 注意AVX-512可能导致CPU降频
- 总是测量性能,避免过度优化

**下一步学习**:
- 《ARM NEON深度剖析》
- 《GPU CUDA优化》
- 《实际项目优化案例》
