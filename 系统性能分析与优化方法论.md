# 系统性能分析与优化方法论

## 课程目录

1. [性能分析的系统化方法](#1-性能分析的系统化方法)
2. [性能分析工具体系](#2-性能分析工具体系)
3. [CPU性能分析](#3-cpu性能分析)
4. [内存和缓存优化](#4-内存和缓存优化)
5. [分支预测和流水线优化](#5-分支预测和流水线优化)
6. [并发和多线程优化](#6-并发和多线程优化)
7. [IO和系统调用优化](#7-io和系统调用优化)
8. [编译器优化技术](#8-编译器优化技术)
9. [SIMD向量化优化](#9-simd向量化优化)
10. [微架构级优化](#10-微架构级优化)
11. [实战：完整的性能优化流程](#11-实战完整的性能优化流程)
12. [性能优化的反模式](#12-性能优化的反模式)

---

## 1. 性能分析的系统化方法

### 1.1 性能优化的黄金法则

```
1. 不要猜测 - 先测量再优化
2. 不要过早优化 - 先保证正确性
3. 不要盲目优化 - 找到真正的瓶颈
4. 不要局部优化 - 考虑整体架构
5. 不要忘记验证 - 对比优化前后
```

### 1.2 性能分析的层次模型

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                 应用层
        (算法、数据结构、业务逻辑)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                 运行时层
        (内存分配、GC、并发控制)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                 编译器层
        (优化选项、内联、循环展开)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                 系统层
        (系统调用、调度、中断)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                 硬件层
        (CPU、缓存、内存、总线)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**分析原则：自上而下，优化自下而上**

### 1.3 系统化性能分析流程

```
┌─────────────────────────────────────────────┐
│ 第1步: 建立性能基线                          │
│ ────────────────────────────────────────    │
│ • 定义性能指标 (延迟/吞吐量/资源利用率)      │
│ • 设计性能测试场景                           │
│ • 记录当前性能数据                           │
│ • 建立可重现的测试环境                       │
└─────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────┐
│ 第2步: 宏观性能剖析 (Profiling)              │
│ ────────────────────────────────────────    │
│ • 使用采样分析器 (perf, VTune)               │
│ • 找到占CPU时间最多的函数                    │
│ • 识别热点代码路径                           │
│ • 分析调用关系和频率                         │
└─────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────┐
│ 第3步: 微观性能分析 (Bottleneck)             │
│ ────────────────────────────────────────    │
│ • CPU Bound: 查看指令执行效率                │
│ • Memory Bound: 查看缓存命中率               │
│ • IO Bound: 查看系统调用和等待时间           │
│ • 使用硬件计数器获取详细指标                 │
└─────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────┐
│ 第4步: 根因分析 (Root Cause)                 │
│ ────────────────────────────────────────    │
│ • 缓存miss: 数据布局、访问模式               │
│ • 分支miss: 条件分支、间接跳转               │
│ • 流水线停顿: 数据依赖、指令延迟             │
│ • 资源竞争: 锁竞争、带宽饱和                 │
└─────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────┐
│ 第5步: 针对性优化 (Optimization)             │
│ ────────────────────────────────────────    │
│ • 算法优化: 降低时间复杂度                   │
│ • 数据结构: 改进访问局部性                   │
│ • 代码级: 循环优化、减少分支                 │
│ • 编译器: 优化选项、手动向量化               │
└─────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────┐
│ 第6步: 验证和迭代 (Validation)               │
│ ────────────────────────────────────────    │
│ • 对比优化前后的性能指标                     │
│ • 验证功能正确性                             │
│ • 检查边界条件和异常情况                     │
│ • 回到第2步继续分析                          │
└─────────────────────────────────────────────┘
```

### 1.4 性能指标的分类

#### 1.4.1 吞吐量指标 (Throughput Metrics)

```
定义: 单位时间内完成的工作量

常见指标:
• QPS (Queries Per Second)      - 每秒查询数
• TPS (Transactions Per Second) - 每秒事务数
• IOPS (IO Operations Per Sec)  - 每秒IO操作数
• Bandwidth (MB/s, GB/s)        - 数据传输速率

测量方法:
throughput = total_operations / total_time
```

#### 1.4.2 延迟指标 (Latency Metrics)

```
定义: 完成单个操作所需的时间

关键百分位数:
• P50 (中位数)   - 50%的请求延迟
• P95            - 95%的请求延迟
• P99            - 99%的请求延迟
• P99.9          - 99.9%的请求延迟 (尾延迟)
• Max            - 最大延迟

为什么关注尾延迟?
在分布式系统中，慢请求会拖累整体性能
```

#### 1.4.3 资源利用率指标 (Utilization Metrics)

```
CPU利用率:
• User CPU   - 用户态CPU使用
• System CPU - 内核态CPU使用
• Wait IO    - 等待IO的CPU时间
• Idle       - 空闲CPU时间

内存指标:
• RSS (Resident Set Size)     - 物理内存使用
• VSZ (Virtual Memory Size)   - 虚拟内存大小
• Page Faults                 - 缺页异常次数
• Swap Usage                  - 交换分区使用

缓存指标:
• L1/L2/L3 Cache Hit Rate     - 缓存命中率
• Cache References/Misses     - 缓存访问/失效次数
• TLB Hit Rate                - TLB命中率
```

#### 1.4.4 效率指标 (Efficiency Metrics)

```
IPC (Instructions Per Cycle):
• 理想值: 接近处理器的发射宽度
  - 标量处理器: IPC ≈ 1
  - 超标量处理器: IPC = 2-4
  - 乱序执行: IPC可能 > 4
• 低IPC表明流水线停顿

CPU时间分解:
Total_Time = Instruction_Count × CPI × Clock_Period

CPI (Cycles Per Instruction) = 1 / IPC

降低执行时间的三种方式:
1. 减少指令数 (算法优化、编译器优化)
2. 降低CPI (减少停顿、提高并行度)
3. 提高时钟频率 (硬件限制)
```

---

## 2. 性能分析工具体系

### 2.1 工具分类和选择

```
┌─────────────────────────────────────────────────────┐
│              性能分析工具金字塔                       │
├─────────────────────────────────────────────────────┤
│                                                     │
│           系统级全局分析                             │
│        ╔═══════════════════╗                       │
│        ║  top / htop       ║  快速查看系统负载      │
│        ║  vmstat / iostat  ║  资源利用率           │
│        ╚═══════════════════╝                       │
│                                                     │
│           采样性能剖析                               │
│        ╔═══════════════════╗                       │
│        ║  perf             ║  通用性能分析         │
│        ║  VTune            ║  Intel微架构分析      │
│        ║  AMD uProf        ║  AMD处理器优化        │
│        ╚═══════════════════╝                       │
│                                                     │
│           插桩和追踪                                 │
│        ╔═══════════════════╗                       │
│        ║  valgrind         ║  内存/缓存分析        │
│        ║  gprof            ║  调用图分析           │
│        ║  strace           ║  系统调用追踪         │
│        ╚═══════════════════╝                       │
│                                                     │
│           专用分析器                                 │
│        ╔═══════════════════╗                       │
│        ║  cachegrind       ║  缓存模拟             │
│        ║  callgrind        ║  调用图和成本         │
│        ║  massif           ║  堆内存分析           │
│        ╚═══════════════════╝                       │
│                                                     │
└─────────────────────────────────────────────────────┘

选择原则:
• 开发阶段: 使用详细分析工具 (Valgrind, VTune)
• 生产环境: 使用低开销工具 (perf, eBPF)
• 初步分析: 采样profiling (perf record)
• 深入分析: 硬件计数器 (perf stat)
```

### 2.2 Linux perf - 性能分析的瑞士军刀

#### 2.2.1 perf的核心概念

```
工作原理:
1. 基于硬件性能计数器 (PMU - Performance Monitoring Unit)
2. 使用采样技术，定期中断CPU记录状态
3. 支持用户态和内核态的统一分析

优势:
• 低开销 (< 1-2%)
• 无需修改代码
• 支持全系统分析
• 丰富的硬件事件
```

#### 2.2.2 perf stat - 统计硬件事件

```bash
# 基础用法：查看程序的硬件计数器
perf stat ./your_program

# 输出示例和解读：
 Performance counter stats for './your_program':

       2,345.67 msec task-clock         # 0.998 CPUs utilized
             12      context-switches   # 5.118 /sec
              2      cpu-migrations     # 0.853 /sec
            234      page-faults        # 99.765 /sec
  8,234,567,890      cycles             # 3.510 GHz
  5,678,901,234      instructions       # 0.69  insn per cycle
  1,234,567,890      branches           # 526.234 M/sec
     12,345,678      branch-misses      # 1.00% of all branches

解读要点:

1. CPUs utilized (CPU利用率)
   0.998 → 接近单核满载
   < 0.9 → 可能有IO等待或锁竞争

2. IPC (Instructions Per Cycle)
   0.69 → 较低，表明性能瓶颈
   理想值:
   • 内存密集: 0.5-1.5
   • 计算密集: 1.5-4.0
   低于0.5 → 严重的停顿

3. Branch Miss Rate
   1.00% → 良好 (< 5%为正常)
   > 10% → 考虑优化分支预测

4. Cache Miss (需指定事件查看)
```

#### 2.2.3 perf stat - 详细的缓存分析

```bash
# 查看详细的缓存事件
perf stat -e \
  cycles,instructions,\
  cache-references,cache-misses,\
  L1-dcache-loads,L1-dcache-load-misses,\
  L1-icache-load-misses,\
  LLC-loads,LLC-load-misses,\
  dTLB-loads,dTLB-load-misses \
  ./your_program

# 输出示例:
  8,234,567,890      cycles
  5,678,901,234      instructions         # 0.69 IPC

  1,234,567,890      cache-references     # 总缓存访问
     98,765,432      cache-misses         # 8.0% miss rate ⚠️

  3,456,789,012      L1-dcache-loads      # L1数据缓存读取
     87,654,321      L1-dcache-load-misses # 2.5% miss rate ✓
      1,234,567      L1-icache-load-misses # L1指令缓存miss

    234,567,890      LLC-loads            # 末级缓存访问
     23,456,789      LLC-load-misses      # 10% miss rate ⚠️

  3,456,789,012      dTLB-loads           # TLB访问
      1,234,567      dTLB-load-misses     # 0.04% miss rate ✓

关键指标判断:

L1 Cache Miss:
✓ < 3%  : 优秀
⚠ 3-10%: 可优化
✗ > 10%: 需要优化数据局部性

LLC (Last Level Cache) Miss:
✓ < 5%  : 优秀
⚠ 5-15%: 可优化
✗ > 15%: 严重的内存瓶颈

TLB Miss:
✓ < 0.1%: 正常
⚠ 0.1-1%: 考虑使用大页
✗ > 1%  : 虚拟内存系统瓶颈
```

#### 2.2.4 perf record + perf report - 热点分析

```bash
# 第1步: 记录性能数据 (采样)
perf record -g -F 99 ./your_program
# -g: 记录调用栈 (call graph)
# -F 99: 采样频率99Hz (每秒99次)

# 第2步: 查看报告
perf report

# perf report 输出解读:
# ────────────────────────────────────────────────────
# Samples: 23K of event 'cycles:u', Event count: 8234567890
# Overhead  Command    Shared Object       Symbol
# ────────────────────────────────────────────────────
   45.23%  program    program             [.] hot_function
   18.67%  program    program             [.] another_hot_func
   12.34%  program    libc-2.31.so        [.] memcpy
    8.90%  program    program             [.] warm_function
    5.67%  program    libm.so.6           [.] exp
    2.34%  program    [kernel.kallsyms]   [k] page_fault
    ...

解读:

1. Overhead (占比)
   45.23% → hot_function 占用了45%的CPU时间
   这是优化的首要目标！

   优化优先级:
   • > 20%: 高优先级
   • 10-20%: 中优先级
   • < 10%: 低优先级

2. Shared Object (所属库)
   program → 你的代码
   libc → 系统库函数
   [kernel] → 内核函数

3. Symbol (函数名)
   [.] → 用户态函数
   [k] → 内核态函数

# 第3步: 查看调用关系
perf report --stdio -g 'graph,0.5,caller'

# 输出示例:
# ────────────────────────────────────────────────────
# 45.23%  hot_function
#         |
#         |--89.45%-- called_from_A
#         |          |
#         |          |--67.89%-- main
#         |          |--32.11%-- worker_thread
#         |
#         |--10.55%-- called_from_B

解读调用链:
• hot_function的89%调用来自called_from_A
• 优化重点: 减少called_from_A中对hot_function的调用次数
```

#### 2.2.5 perf annotate - 源码级分析

```bash
# 查看具体函数的汇编代码和性能
perf annotate hot_function

# 输出示例:
# ────────────────────────────────────────────────────
#  Percent |  Source code & Disassembly
# ────────────────────────────────────────────────────
#          : Disassembly of section .text:
#          :
#          : 0000000000001234 <hot_function>:
#          : hot_function():
#    0.12  :   push   %rbp
#    0.08  :   mov    %rsp,%rbp
#   45.67  :   mov    0x10(%rdi),%rax    ← 高开销!
#   23.45  :   imul   $0x5,%rax,%rax    ← 计算密集
#   15.32  :   cmp    %rax,%rsi
#    8.90  :   jle    1250 <hot_function+0x1c>
#    2.34  :   mov    %rax,0x18(%rdi)
#    0.12  :   pop    %rbp
#    0.00  :   ret

解读热点指令:

1. 内存访问 (45.67%)
   mov 0x10(%rdi),%rax
   → 可能的cache miss
   → 考虑: 预取、数据重排

2. 计算操作 (23.45%)
   imul $0x5,%rax,%rax
   → 乘法指令延迟高
   → 考虑: 改用位运算、查表

3. 分支 (8.90%)
   jle (条件跳转)
   → 可能的分支预测失败
   → 考虑: 改写为无分支代码
```

#### 2.2.6 perf top - 实时监控

```bash
# 实时显示系统热点
perf top -g

# 输出示例:
# ────────────────────────────────────────────────────
# Samples: 8K of event 'cycles', 4000 Hz, Event count: 32456789
# Overhead  Shared Object       Symbol
# ────────────────────────────────────────────────────
   25.67%  [kernel]            [k] copy_user_generic_string
   18.90%  program             [.] hot_function
   12.34%  libc-2.31.so        [.] __memcpy_avx_unaligned
    8.45%  [kernel]            [k] _raw_spin_lock
    6.78%  program             [.] process_data
    ...

用途:
• 快速找到系统级热点
• 实时监控性能变化
• 发现意外的热点函数
```

### 2.3 Intel VTune Profiler - 微架构分析

#### 2.3.1 Top-Down微架构分析方法

```
CPU流水线利用率分解:

              100% CPU Time
                    |
        ┌───────────┴───────────┐
        |                       |
    Retiring               Bad Speculation
  (有效工作)                (投机失败)
     35%                      15%
        |                       |
        |                   ┌───┴────┐
        |                Branch   Machine
        |                Mispredict Clears
        |                  12%      3%
        |
        |           ┌──────────────────────┐
        |           |                      |
    Frontend Bound              Backend Bound
   (前端瓶颈)                    (后端瓶颈)
       20%                         30%
        |                           |
    ┌───┴────┐              ┌──────┴──────┐
  Fetch    Decode        Core        Memory
  Latency  Latency       Bound       Bound
   10%      10%          10%          20%
                                       |
                                 ┌─────┴─────┐
                              L1    L2/L3   DRAM
                              Bound  Bound  Bound
                              3%     7%     10%

理想分布:
• Retiring > 70%        (大部分时间在做有用的工作)
• Backend Bound < 20%   (后端执行单元不饱和)
• Bad Speculation < 10% (分支预测准确)

瓶颈诊断:
• Frontend Bound > 20%  → 指令获取/解码瓶颈
  - 改进代码布局
  - 减少代码大小
  - 对齐热路径

• Backend Core Bound > 30% → 计算单元饱和
  - 已经接近硬件极限
  - 考虑算法优化

• Backend Memory Bound > 30% → 内存瓶颈
  - 优化缓存利用
  - 减少内存访问
  - 使用预取指令
```

#### 2.3.2 VTune使用流程

```bash
# 1. Hotspots分析 (快速定位热点)
vtune -collect hotspots -result-dir vtune_hotspots ./program

# 2. 微架构探索 (深入分析)
vtune -collect uarch-exploration -result-dir vtune_uarch ./program

# 3. 内存访问分析
vtune -collect memory-access -result-dir vtune_memory ./program

# 4. 查看报告
vtune -report summary -result-dir vtune_uarch

# 输出示例:
# ────────────────────────────────────────────────────
# Top Hotspots
# Function                          CPU Time    % of Total
# ────────────────────────────────────────────────────
# hot_function                      15.234s     45.2%
# process_data                      8.456s      25.1%
# memcpy                            4.123s      12.2%
#
# Pipeline Slots Distribution:
# ────────────────────────────────────────────────────
# Retiring                          35.2%
# Bad Speculation                   15.8%
#   Branch Mispredicts              12.3%
#   Machine Clears                   3.5%
# Frontend Bound                    18.9%
#   Fetch Latency                    9.2%
#   Fetch Bandwidth                  9.7%
# Backend Bound                     30.1%  ← 主要瓶颈!
#   Core Bound                       8.7%
#   Memory Bound                    21.4%  ← 内存问题!
#     L1 Bound                       2.3%
#     L2 Bound                       4.5%
#     L3 Bound                       6.8%
#     DRAM Bound                     7.8%  ← 主要问题!

# 5. GUI查看
vtune-gui vtune_uarch
```

### 2.4 Valgrind Cachegrind - 缓存模拟器

```bash
# 运行缓存模拟
valgrind --tool=cachegrind \
         --cache-sim=yes \
         --branch-sim=yes \
         ./program

# 输出文件: cachegrind.out.<pid>

# 查看统计
cg_annotate cachegrind.out.<pid>

# 输出示例和解读:
# ────────────────────────────────────────────────────
# I   refs:      1,234,567,890    # 指令引用
# I1  misses:        1,234,567    # L1指令缓存miss
# LLi misses:          123,456    # 末级指令缓存miss
# I1  miss rate:         0.10%    # ✓ 指令缓存命中率高
# LLi miss rate:         0.01%    # ✓
#
# D   refs:      5,678,901,234    # 数据引用
# D1  misses:      123,456,789    # L1数据缓存miss
# LLd misses:       12,345,678    # 末级数据缓存miss
# D1  miss rate:         2.2%     # ✓ 可接受
# LLd miss rate:         0.2%     # ✓
#
# LL refs:         124,691,356
# LL misses:        12,469,134
# LL miss rate:          0.2%     # ✓

优化目标:
• L1 miss rate < 3%
• LL miss rate < 1%

# 查看每个函数的缓存表现
cg_annotate --auto=yes cachegrind.out.<pid>

# 输出示例:
# ────────────────────────────────────────────────────
# Ir          I1mr  ILmr    Dr          D1mr    DLmr    Function
# ────────────────────────────────────────────────────
# 456,789,012   123    45  234,567,890  12,345  1,234  hot_function
# 123,456,789    34    12   89,012,345   8,901    890  process_data
#
# Ir:   指令读取
# I1mr: L1指令miss
# ILmr: LL指令miss
# Dr:   数据读取
# D1mr: L1数据miss
# DLmr: LL数据miss

分析要点:
hot_function:
• D1mr/Dr = 12345/234567890 = 0.0053% → 优秀的L1命中率
• DLmr/Dr = 1234/234567890 = 0.0005%  → 优秀的LL命中率

如果miss率高:
• 检查数据结构大小 (是否超出缓存)
• 检查访问模式 (是否随机访问)
• 考虑重组数据布局
```

---

## 3. CPU性能分析

### 3.1 识别CPU瓶颈

#### 3.1.1 使用perf判断CPU瓶颈类型

```bash
# 综合分析
perf stat -e cycles,instructions,\
  cache-references,cache-misses,\
  branches,branch-misses,\
  stalled-cycles-frontend,stalled-cycles-backend \
  ./program

# 输出示例:
# ────────────────────────────────────────────────────
  8,234,567,890    cycles
  4,567,890,123    instructions           # 0.55 IPC

  1,234,567,890    cache-references
    234,567,890    cache-misses           # 19.0% miss ⚠️

  1,234,567,890    branches
     45,678,901    branch-misses          # 3.7%  ✓

  3,456,789,012    stalled-cycles-frontend  # 42.0% frontend
  4,123,456,789    stalled-cycles-backend   # 50.1% backend ⚠️

诊断决策树:

1. IPC < 1.0 且 Backend Stalled > 40%
   → Memory Bound
   → 检查cache miss率

2. IPC < 1.0 且 Frontend Stalled > 30%
   → Frontend Bound
   → 指令获取瓶颈
   → 检查代码大小、对齐

3. IPC > 2.0 且 Cache Miss < 5%
   → CPU Compute Bound
   → 接近硬件极限
   → 考虑算法优化、并行化

4. Branch Miss > 10%
   → Branch Misprediction
   → 优化分支预测
```

### 3.2 CPU Compute Bound 优化

#### 3.2.1 减少指令数

```cpp
// 反例: 不必要的计算
void bad_example(int* arr, int n) {
    for (int i = 0; i < n; i++) {
        arr[i] = arr[i] * 2 / 2;  // 无效计算
    }
}

// 优化: 消除冗余
void good_example(int* arr, int n) {
    // 编译器优化通常能识别并消除
    // 但最好从源头避免
}

// 反例: 循环内的不变计算
void bad_loop(float* arr, int n, float x, float y) {
    for (int i = 0; i < n; i++) {
        arr[i] = arr[i] * (x * y);  // x*y每次都计算
    }
}

// 优化: 循环不变量外提
void good_loop(float* arr, int n, float x, float y) {
    float xy = x * y;  // 提到循环外
    for (int i = 0; i < n; i++) {
        arr[i] = arr[i] * xy;
    }
}

验证优化效果:
perf stat -e instructions ./program_before
perf stat -e instructions ./program_after

对比 instructions 计数
```

#### 3.2.2 提高指令级并行 (ILP)

```cpp
// 反例: 数据依赖链
void bad_dependency(int* arr, int n) {
    int sum = 0;
    for (int i = 0; i < n; i++) {
        sum += arr[i];  // 每次依赖前一次的sum
    }
}

// 优化: 循环展开 + 多累加器
void good_parallel(int* arr, int n) {
    int sum0 = 0, sum1 = 0, sum2 = 0, sum3 = 0;
    int i;

    // 4路展开
    for (i = 0; i + 3 < n; i += 4) {
        sum0 += arr[i];
        sum1 += arr[i+1];
        sum2 += arr[i+2];
        sum3 += arr[i+3];
    }

    // 处理剩余
    int sum = sum0 + sum1 + sum2 + sum3;
    for (; i < n; i++) {
        sum += arr[i];
    }
}

原理:
• 打破数据依赖链
• 让CPU能同时执行多个加法
• IPC可以提升2-4倍

验证:
perf stat -e cycles,instructions ./program
# 观察IPC变化
```

#### 3.2.3 使用查表替代计算

```cpp
// 反例: 重复的复杂计算
float bad_compute(int x) {
    return sin(x * 0.1) + cos(x * 0.1);  // 每次都计算
}

void process(int* data, int n) {
    for (int i = 0; i < n; i++) {
        data[i] = bad_compute(data[i]) * 1000;
    }
}

// 优化: 预计算查找表
#define TABLE_SIZE 1000
float lookup_table[TABLE_SIZE];

void init_table() {
    for (int i = 0; i < TABLE_SIZE; i++) {
        lookup_table[i] = sin(i * 0.1) + cos(i * 0.1);
    }
}

float good_lookup(int x) {
    return lookup_table[x % TABLE_SIZE];
}

void process_optimized(int* data, int n) {
    for (int i = 0; i < n; i++) {
        data[i] = good_lookup(data[i]) * 1000;
    }
}

权衡:
• 优点: 速度快 (内存访问 vs 复杂计算)
• 缺点: 占用内存, 精度可能降低
• 适用: 输入范围有限, 计算密集

测量:
perf stat -e cycles,cache-references,cache-misses \
  ./program_before ./program_after
```

### 3.3 Frontend Bound 优化

#### 3.3.1 代码布局优化

```cpp
// 反例: 热路径和冷路径混杂
void bad_layout(int* data, int n) {
    for (int i = 0; i < n; i++) {
        if (likely_condition(data[i])) {
            // 热路径
            fast_process(data[i]);
        } else {
            // 冷路径 (极少执行)
            rare_error_handling(data[i]);
        }
    }
}

// 优化: 使用likely/unlikely hint
void good_layout(int* data, int n) {
    for (int i = 0; i < n; i++) {
        if (__builtin_expect(likely_condition(data[i]), 1)) {
            fast_process(data[i]);
        } else {
            rare_error_handling(data[i]);
        }
    }
}

// 或者重构代码结构
void better_layout(int* data, int n) {
    for (int i = 0; i < n; i++) {
        // 热路径集中
        if (likely_condition(data[i])) {
            fast_process(data[i]);
        }
    }
    // 冷路径分离
    for (int i = 0; i < n; i++) {
        if (!likely_condition(data[i])) {
            rare_error_handling(data[i]);
        }
    }
}

编译器选项:
gcc -O3 -fprofile-generate ./program  # 收集profile
./program < typical_input              # 运行获取profile
gcc -O3 -fprofile-use ./program        # 使用profile优化

效果: 编译器会根据profile优化代码布局
```

#### 3.3.2 函数内联

```cpp
// 小函数反复调用
inline int add(int a, int b) {
    return a + b;
}

void compute(int* arr, int n) {
    for (int i = 0; i < n; i++) {
        arr[i] = add(arr[i], i);  // 调用开销
    }
}

编译器优化:
• -O2/-O3 自动内联小函数
• __attribute__((always_inline)) 强制内联
• __attribute__((noinline)) 禁止内联

查看内联情况:
gcc -O3 -S -fverbose-asm program.c
# 查看汇编代码

或使用:
perf record -e instructions ./program
perf report
# 查看是否有小函数占用比例
```

---

## 4. 内存和缓存优化

### 4.1 理解内存层次结构

```
寄存器 (Registers)
    ↓ < 1 cycle
L1 Cache (32-64 KB per core)
    ↓ ~4 cycles (1-2 ns)
L2 Cache (256KB-1MB per core)
    ↓ ~12 cycles (3-7 ns)
L3 Cache (8-64MB shared)
    ↓ ~40 cycles (10-20 ns)
Main Memory (DRAM)
    ↓ ~200 cycles (50-100 ns)
SSD
    ↓ ~50,000 cycles (10-100 μs)
HDD
    ↓ ~10,000,000 cycles (5-20 ms)

性能差距:
L1 Cache vs DRAM: 50x
DRAM vs SSD: 1000x
SSD vs HDD: 100x

关键原则: 访问速度差异巨大，必须优化访问模式！
```

### 4.2 缓存友好的数据结构

#### 4.2.1 数组 vs 链表

```cpp
// 反例: 链表 - 缓存不友好
struct Node {
    int data;
    Node* next;  // 随机内存访问
};

void sum_list(Node* head) {
    int sum = 0;
    for (Node* p = head; p != nullptr; p = p->next) {
        sum += p->data;  // 每次访问都可能cache miss
    }
}

// 优化: 数组 - 缓存友好
void sum_array(int* arr, int n) {
    int sum = 0;
    for (int i = 0; i < n; i++) {
        sum += arr[i];  // 连续访问, 预取有效
    }
}

性能对比测试:
#include <chrono>
#include <iostream>

const int N = 10000000;

// 测试链表
Node* create_list(int n) {
    Node* head = new Node{0, nullptr};
    Node* curr = head;
    for (int i = 1; i < n; i++) {
        curr->next = new Node{i, nullptr};
        curr = curr->next;
    }
    return head;
}

int benchmark_list() {
    Node* list = create_list(N);
    auto start = std::chrono::high_resolution_clock::now();

    int sum = 0;
    for (Node* p = list; p != nullptr; p = p->next) {
        sum += p->data;
    }

    auto end = std::chrono::high_resolution_clock::now();
    return std::chrono::duration_cast<std::chrono::milliseconds>(
        end - start).count();
}

// 测试数组
int benchmark_array() {
    int* arr = new int[N];
    for (int i = 0; i < N; i++) arr[i] = i;

    auto start = std::chrono::high_resolution_clock::now();

    int sum = 0;
    for (int i = 0; i < N; i++) {
        sum += arr[i];
    }

    auto end = std::chrono::high_resolution_clock::now();
    delete[] arr;
    return std::chrono::duration_cast<std::chrono::milliseconds>(
        end - start).count();
}

使用perf验证:
perf stat -e cache-references,cache-misses ./test_list
perf stat -e cache-references,cache-misses ./test_array

典型结果:
链表: cache miss rate ~15-20%
数组: cache miss rate ~1-2%
速度差距: 5-10x
```

#### 4.2.2 Structure of Arrays (SoA) vs Array of Structures (AoS)

```cpp
// Array of Structures (AoS) - 可能不够缓存友好
struct Particle {
    float x, y, z;     // 位置
    float vx, vy, vz;  // 速度
    float mass;
    int id;
};

void update_positions_aos(Particle* particles, int n, float dt) {
    for (int i = 0; i < n; i++) {
        particles[i].x += particles[i].vx * dt;  // 访问跨度大
        particles[i].y += particles[i].vy * dt;
        particles[i].z += particles[i].vz * dt;
    }
}

// Structure of Arrays (SoA) - 缓存友好
struct ParticlesSoA {
    float* x;   // 连续的x数组
    float* y;   // 连续的y数组
    float* z;
    float* vx;
    float* vy;
    float* vz;
    float* mass;
    int* id;
};

void update_positions_soa(ParticlesSoA& particles, int n, float dt) {
    for (int i = 0; i < n; i++) {
        particles.x[i] += particles.vx[i] * dt;  // 连续访问
        particles.y[i] += particles.vy[i] * dt;
        particles.z[i] += particles.vz[i] * dt;
    }
}

何时使用SoA:
• 经常只访问结构体的部分字段
• 需要向量化 (SIMD)
• 数据量大

何时使用AoS:
• 经常需要访问整个结构体
• 结构体较小 (< 64 bytes)
• 需要保持数据完整性

验证:
perf stat -e L1-dcache-load-misses ./aos_version
perf stat -e L1-dcache-load-misses ./soa_version
```

### 4.3 减少缓存冲突

#### 4.3.1 缓存行填充 (Cache Line Padding)

```cpp
// 反例: False Sharing
struct Counter {
    std::atomic<int> count;  // 4 bytes
};

Counter counters[4];  // 多线程各自更新

void worker(int id) {
    for (int i = 0; i < 1000000; i++) {
        counters[id].count++;  // False sharing!
    }
}

问题:
• 4个Counter可能在同一个缓存行 (64 bytes)
• 每次写入导致其他CPU的缓存行失效
• 严重的缓存一致性开销

// 优化: 缓存行对齐
struct alignas(64) Counter {
    std::atomic<int> count;
    char padding[60];  // 填充到64字节
};

或使用C++17:
struct Counter {
    alignas(std::hardware_destructive_interference_size)
    std::atomic<int> count;
};

验证false sharing:
perf c2c record ./program
perf c2c report
```

### 4.4 预取优化

#### 4.4.1 软件预取

```cpp
#include <xmmintrin.h>  // SSE prefetch

// 手动预取
void process_with_prefetch(int* arr, int n) {
    for (int i = 0; i < n; i++) {
        // 预取未来的数据
        if (i + 8 < n) {
            _mm_prefetch((char*)&arr[i + 8], _MM_HINT_T0);
            // T0: 预取到L1
            // T1: 预取到L2
            // T2: 预取到L3
            // NTA: Non-temporal (不污染缓存)
        }

        // 处理当前数据
        process(arr[i]);
    }
}

// 对于指针追踪
void list_with_prefetch(Node* head) {
    Node* curr = head;
    Node* next = curr ? curr->next : nullptr;

    while (curr) {
        // 预取下一个节点
        if (next && next->next) {
            _mm_prefetch((char*)next->next, _MM_HINT_T0);
        }

        process(curr->data);
        curr = next;
        next = curr ? curr->next : nullptr;
    }
}

注意:
• 预取距离需要调优 (太近无效, 太远浪费)
• 典型值: 8-32个元素
• 使用perf验证效果
```

### 4.5 大页 (Huge Pages) 优化

```cpp
// TLB (Translation Lookaside Buffer) 优化

// 普通页: 4KB
// 大页: 2MB (x86-64)
// 巨页: 1GB (x86-64)

// 启用透明大页 (Linux)
// echo always > /sys/kernel/mm/transparent_hugepage/enabled

// 显式分配大页
#include <sys/mman.h>

void* allocate_hugepage(size_t size) {
    void* ptr = mmap(
        nullptr,
        size,
        PROT_READ | PROT_WRITE,
        MAP_PRIVATE | MAP_ANONYMOUS | MAP_HUGETLB,
        -1,
        0
    );

    if (ptr == MAP_FAILED) {
        perror("mmap hugepage failed");
        return nullptr;
    }

    return ptr;
}

何时使用大页:
• 大内存分配 (> 100MB)
• 频繁的随机内存访问
• TLB miss率高 (> 1%)

验证效果:
perf stat -e dTLB-load-misses,dTLB-store-misses ./program

优化前: TLB miss ~2%
优化后: TLB miss ~0.1%
```

---

## 5. 分支预测和流水线优化

### 5.1 理解分支预测

```
现代CPU流水线:
┌──────┬───────┬────────┬─────────┬─────────┬──────────┐
│ Fetch│ Decode│ Execute│ Memory  │Writeback│  ...     │
└──────┴───────┴────────┴─────────┴─────────┴──────────┘

分支指令的问题:
• CPU需要提前fetch下一条指令
• 遇到分支不知道跳转还是不跳转
• 预测错误会导致流水线清空 (15-20 cycles惩罚)

分支预测器类型:
1. 静态预测: 基于指令类型
   • 向后跳转 (循环): 预测跳转
   • 向前跳转: 预测不跳转

2. 动态预测: 基于历史
   • 单bit预测器
   • 双bit预测器 (饱和计数器)
   • 全局历史预测器
   • 混合预测器
```

### 5.2 优化分支预测

#### 5.2.1 消除不可预测的分支

```cpp
// 反例: 数据相关的条件分支
int sum_positive(int* arr, int n) {
    int sum = 0;
    for (int i = 0; i < n; i++) {
        if (arr[i] > 0) {  // 不可预测
            sum += arr[i];
        }
    }
    return sum;
}

// 优化1: 使用条件移动 (CMOV)
int sum_positive_cmov(int* arr, int n) {
    int sum = 0;
    for (int i = 0; i < n; i++) {
        int val = arr[i];
        int add = (val > 0) ? val : 0;  // 编译器可能生成CMOV
        sum += add;
    }
    return sum;
}

// 优化2: 位运算技巧
int sum_positive_bitwise(int* arr, int n) {
    int sum = 0;
    for (int i = 0; i < n; i++) {
        int val = arr[i];
        int mask = val >> 31;  // 负数: 全1, 非负: 全0
        sum += val & ~mask;    // 负数被mask掉
    }
    return sum;
}

验证分支预测性能:
perf stat -e branches,branch-misses ./program

Branch miss < 5%: 良好
Branch miss > 10%: 需要优化
```

#### 5.2.2 提高分支可预测性

```cpp
// 反例: 随机数据导致分支不可预测
void process_random(int* data, int n) {
    for (int i = 0; i < n; i++) {
        if (data[i] % 2 == 0) {  // 50% true, 50% false
            even_process(data[i]);
        } else {
            odd_process(data[i]);
        }
    }
}

// 优化: 先排序使分支可预测
void process_sorted(int* data, int n) {
    // 先分组
    std::partition(data, data + n, [](int x) { return x % 2 == 0; });

    // 现在分支可预测了
    for (int i = 0; i < n; i++) {
        if (data[i] % 2 == 0) {  // 前半部分全是true
            even_process(data[i]);
        } else {  // 后半部分全是false
            odd_process(data[i]);
        }
    }
}

// 更好: 完全消除分支
void process_branchless(int* data, int n) {
    // 分成两个循环
    int split = std::partition(data, data + n,
                               [](int x) { return x % 2 == 0; }) - data;

    for (int i = 0; i < split; i++) {
        even_process(data[i]);  // 无分支
    }
    for (int i = split; i < n; i++) {
        odd_process(data[i]);   // 无分支
    }
}

测试案例:
const int N = 1000000;
int* random_data = new int[N];
for (int i = 0; i < N; i++) {
    random_data[i] = rand();
}

perf stat -e branch-misses ./process_random    # ~50% miss
perf stat -e branch-misses ./process_sorted    # ~1% miss
```

#### 5.2.3 使用分支提示

```cpp
// GCC/Clang 分支提示
#define likely(x)   __builtin_expect(!!(x), 1)
#define unlikely(x) __builtin_expect(!!(x), 0)

// 应用
void error_handling(int* data, int n) {
    for (int i = 0; i < n; i++) {
        if (unlikely(data[i] < 0)) {  // 错误情况很少见
            handle_error(data[i]);
        }

        if (likely(data[i] < MAX_VALUE)) {  // 正常情况
            normal_process(data[i]);
        }
    }
}

效果:
• 编译器会将unlikely的代码移到函数末尾
• 提高指令缓存效率
• 帮助CPU分支预测器

注意:
• 只用于非常确定的情况 (95%以上)
• 滥用反而降低性能
• 使用PGO (Profile-Guided Optimization) 更可靠
```

### 5.3 Profile-Guided Optimization (PGO)

```bash
# 三步PGO流程

# 第1步: 编译插桩版本
g++ -O3 -fprofile-generate -o program program.cpp

# 第2步: 运行收集profile数据
./program < typical_workload_1.txt
./program < typical_workload_2.txt
./program < typical_workload_3.txt
# 生成 program.gcda 文件

# 第3步: 使用profile重新编译
g++ -O3 -fprofile-use -o program_optimized program.cpp

# 对比性能
perf stat ./program
perf stat ./program_optimized

PGO的优化:
1. 代码布局优化
   • 热代码集中放置
   • 冷代码移到末尾

2. 内联决策
   • 热路径函数内联
   • 冷路径函数不内联

3. 分支预测优化
   • 调整分支顺序
   • 优化循环展开

4. 寄存器分配
   • 热变量优先分配寄存器

典型提升: 5-15%
```

---

## 6. 并发和多线程优化

### 6.1 并行扩展性分析

#### 6.1.1 Amdahl定律实践

```cpp
// 测量串行部分和并行部分

#include <omp.h>
#include <chrono>

void analyze_scalability() {
    const int N = 10000000;
    int* data = new int[N];

    // 测试不同线程数
    for (int threads = 1; threads <= 32; threads *= 2) {
        omp_set_num_threads(threads);

        auto start = std::chrono::high_resolution_clock::now();

        // 串行初始化 (无法并行)
        for (int i = 0; i < N; i++) {
            data[i] = i;
        }

        auto mid = std::chrono::high_resolution_clock::now();

        // 并行计算
        long long sum = 0;
        #pragma omp parallel for reduction(+:sum)
        for (int i = 0; i < N; i++) {
            sum += data[i] * data[i];
        }

        auto end = std::chrono::high_resolution_clock::now();

        double serial_time = std::chrono::duration<double>(
            mid - start).count();
        double parallel_time = std::chrono::duration<double>(
            end - mid).count();
        double total_time = serial_time + parallel_time;

        printf("Threads=%2d: Serial=%.3fs, Parallel=%.3fs, "
               "Total=%.3fs, Speedup=%.2fx, Efficiency=%.1f%%\n",
               threads, serial_time, parallel_time, total_time,
               baseline_time / total_time,
               (baseline_time / total_time) / threads * 100);
    }

    delete[] data;
}

典型输出:
Threads= 1: Serial=0.050s, Parallel=0.200s, Total=0.250s, Speedup=1.00x, Efficiency=100.0%
Threads= 2: Serial=0.050s, Parallel=0.105s, Total=0.155s, Speedup=1.61x, Efficiency=80.6%
Threads= 4: Serial=0.050s, Parallel=0.058s, Total=0.108s, Speedup=2.31x, Efficiency=57.9%
Threads= 8: Serial=0.050s, Parallel=0.035s, Total=0.085s, Speedup=2.94x, Efficiency=36.8%

分析:
• 串行部分占比 = 0.050 / 0.250 = 20%
• 理论最大加速比 = 1 / 0.20 = 5x
• 8线程时已接近极限
```

### 6.2 锁竞争分析

#### 6.2.1 使用perf检测锁竞争

```bash
# 记录锁相关事件
perf record -e 'sched:sched_switch' -a -g ./program

# 查看报告
perf report --sort comm,dso,symbol

# 查找锁竞争
perf script | grep -A 5 -B 5 "pthread_mutex_lock"

# 使用perf lock
perf lock record ./program
perf lock report

# 输出示例:
# Name               acquired  contended  avg wait(ns)  max wait(ns)
# ─────────────────────────────────────────────────────────────────
# &lock1              1234567      45678        12345       456789
# &lock2               234567       8901         5678        89012
```

#### 6.2.2 减少锁竞争

```cpp
// 反例: 单一全局锁
std::mutex global_mutex;
int global_counter = 0;

void worker_bad() {
    for (int i = 0; i < 1000000; i++) {
        std::lock_guard<std::mutex> lock(global_mutex);
        global_counter++;  // 严重竞争!
    }
}

// 优化1: 线程局部计数 + 最后合并
void worker_tls() {
    thread_local int local_counter = 0;

    for (int i = 0; i < 1000000; i++) {
        local_counter++;  // 无锁
    }

    // 最后一次性更新
    std::lock_guard<std::mutex> lock(global_mutex);
    global_counter += local_counter;
}

// 优化2: 原子操作 (适合简单操作)
std::atomic<int> atomic_counter{0};

void worker_atomic() {
    for (int i = 0; i < 1000000; i++) {
        atomic_counter.fetch_add(1, std::memory_order_relaxed);
    }
}

// 优化3: 分段锁
const int NUM_SEGMENTS = 64;
struct alignas(64) Segment {
    std::mutex mutex;
    int counter;
    char padding[60];  // 避免false sharing
};

Segment segments[NUM_SEGMENTS];

void worker_segmented(int thread_id) {
    int seg_id = thread_id % NUM_SEGMENTS;
    for (int i = 0; i < 1000000; i++) {
        std::lock_guard<std::mutex> lock(segments[seg_id].mutex);
        segments[seg_id].counter++;
    }
}

性能对比 (8线程):
全局锁:    8.5s
TLS累积:   0.12s  (70x faster)
原子操作:  0.45s  (19x faster)
分段锁:    1.2s   (7x faster)
```

### 6.3 NUMA感知优化

```cpp
// 检查NUMA拓扑
#include <numa.h>

void print_numa_info() {
    if (numa_available() < 0) {
        printf("NUMA not available\n");
        return;
    }

    int num_nodes = numa_num_configured_nodes();
    printf("NUMA nodes: %d\n", num_nodes);

    for (int i = 0; i < num_nodes; i++) {
        long long size = numa_node_size64(i, nullptr);
        printf("Node %d: %lld MB\n", i, size / (1024*1024));
    }
}

// NUMA感知的内存分配
void* allocate_on_node(size_t size, int node) {
    void* ptr = numa_alloc_onnode(size, node);
    return ptr;
}

// 绑定线程到NUMA节点
void bind_thread_to_node(int node) {
    cpu_set_t cpuset;
    CPU_ZERO(&cpuset);

    // 获取该node上的CPU
    struct bitmask* cpus = numa_allocate_cpumask();
    numa_node_to_cpus(node, cpus);

    for (int i = 0; i < numa_num_possible_cpus(); i++) {
        if (numa_bitmask_isbitset(cpus, i)) {
            CPU_SET(i, &cpuset);
        }
    }

    pthread_setaffinity_np(pthread_self(), sizeof(cpuset), &cpuset);
    numa_free_cpumask(cpus);
}

// NUMA感知的并行算法
void parallel_process_numa(int* data, int n, int num_threads) {
    int num_nodes = numa_num_configured_nodes();
    int chunk_size = n / num_threads;

    #pragma omp parallel num_threads(num_threads)
    {
        int tid = omp_get_thread_num();
        int node = tid % num_nodes;

        // 绑定到NUMA节点
        bind_thread_to_node(node);

        // 处理数据
        int start = tid * chunk_size;
        int end = (tid == num_threads - 1) ? n : start + chunk_size;

        for (int i = start; i < end; i++) {
            process(data[i]);
        }
    }
}

验证NUMA效果:
numactl --hardware
numactl --cpubind=0 --membind=0 ./program  # 绑定node 0
perf stat -e 'node-loads,node-load-misses,node-stores,node-store-misses' \
  ./program
```

---

## 7. IO和系统调用优化

### 7.1 减少系统调用

```cpp
// 反例: 频繁的小IO
void write_bad(int fd, const char* data, int n) {
    for (int i = 0; i < n; i++) {
        write(fd, &data[i], 1);  // n次系统调用!
    }
}

// 优化: 批量IO
void write_good(int fd, const char* data, int n) {
    write(fd, data, n);  // 1次系统调用
}

// 使用缓冲IO
void write_buffered(const char* filename, const char* data, int n) {
    FILE* fp = fopen(filename, "w");
    setvbuf(fp, nullptr, _IOFBF, 64*1024);  // 64KB缓冲

    for (int i = 0; i < n; i++) {
        fputc(data[i], fp);  // 库函数缓冲，减少系统调用
    }

    fclose(fp);
}

测量系统调用开销:
strace -c ./program

输出:
% time     seconds  usecs/call     calls    errors syscall
------ ----------- ----------- --------- --------- -------
 45.23    0.012345           5      2345           write
 23.45    0.006789          10       678           read
 12.34    0.003456         123        28           open
  8.90    0.002567           8       321           close
```

### 7.2 异步IO

```cpp
// 同步IO - 阻塞
void sync_read(int fd, char* buf, size_t size) {
    ssize_t n = read(fd, buf, size);  // 阻塞直到数据到达
}

// 异步IO (io_uring - Linux 5.1+)
#include <liburing.h>

void async_read_uring(int fd, char* buf, size_t size) {
    struct io_uring ring;
    io_uring_queue_init(32, &ring, 0);

    struct io_uring_sqe* sqe = io_uring_get_sqe(&ring);
    io_uring_prep_read(sqe, fd, buf, size, 0);
    io_uring_submit(&ring);

    // 可以继续做其他工作
    do_other_work();

    // 等待完成
    struct io_uring_cqe* cqe;
    io_uring_wait_cqe(&ring, &cqe);

    ssize_t result = cqe->res;
    io_uring_cqe_seen(&ring, cqe);
    io_uring_queue_exit(&ring);
}

性能对比:
• 同步IO: 阻塞等待，CPU空闲
• 异步IO: 立即返回，CPU可做其他工作
• 适用场景: 大量IO操作，可并发处理
```

### 7.3 内存映射文件 (mmap)

```cpp
// 传统文件读取
void traditional_read(const char* filename) {
    int fd = open(filename, O_RDONLY);
    struct stat st;
    fstat(fd, &st);

    char* buffer = new char[st.st_size];
    read(fd, buffer, st.st_size);  // 拷贝到用户空间

    // 处理数据
    process(buffer, st.st_size);

    delete[] buffer;
    close(fd);
}

// mmap - 零拷贝
void mmap_read(const char* filename) {
    int fd = open(filename, O_RDONLY);
    struct stat st;
    fstat(fd, &st);

    // 映射文件到内存
    char* data = (char*)mmap(
        nullptr, st.st_size,
        PROT_READ, MAP_PRIVATE,
        fd, 0
    );

    // 建议内核预读
    madvise(data, st.st_size, MADV_SEQUENTIAL);

    // 直接访问，无需拷贝
    process(data, st.st_size);

    munmap(data, st.st_size);
    close(fd);
}

优势:
• 减少内存拷贝
• 利用页缓存
• 懒加载 (按需分页)

适用场景:
• 大文件 (> 1MB)
• 随机访问
• 多进程共享数据
```

---

## 8. 编译器优化技术

### 8.1 编译器优化级别

```bash
# GCC/Clang优化级别对比

# -O0: 无优化 (默认)
g++ -O0 program.cpp -o program_O0

# -O1: 基础优化
# • 常量折叠
# • 死代码消除
# • 简单内联

# -O2: 推荐优化 (生产环境)
g++ -O2 program.cpp -o program_O2
# • -O1的所有优化
# • 循环优化
# • 指令调度
# • CSE (公共子表达式消除)
# • 不影响编译时间和调试

# -O3: 激进优化
g++ -O3 program.cpp -o program_O3
# • -O2的所有优化
# • 函数内联
# • 循环向量化
# • 循环展开
# • 可能增加代码大小

# -Ofast: 最激进 (可能破坏标准兼容性)
g++ -Ofast program.cpp -o program_Ofast
# • -O3 + 快速数学
# • 忽略IEEE浮点标准
# • 不安全的优化假设

# -Os: 优化大小
g++ -Os program.cpp -o program_Os
# 嵌入式系统，代码大小敏感

性能对比测试:
perf stat ./program_O0  # baseline
perf stat ./program_O2  # 通常2-5x faster
perf stat ./program_O3  # 通常比O2快10-30%
perf stat ./program_Ofast  # 可能再快5-15%
```

### 8.2 循环优化

#### 8.2.1 循环展开

```cpp
// 原始循环
void sum_normal(int* arr, int n) {
    int sum = 0;
    for (int i = 0; i < n; i++) {
        sum += arr[i];
    }
}

// 手动展开4路
void sum_unrolled(int* arr, int n) {
    int sum0 = 0, sum1 = 0, sum2 = 0, sum3 = 0;
    int i;

    for (i = 0; i + 3 < n; i += 4) {
        sum0 += arr[i];
        sum1 += arr[i+1];
        sum2 += arr[i+2];
        sum3 += arr[i+3];
    }

    int sum = sum0 + sum1 + sum2 + sum3;

    // 处理余数
    for (; i < n; i++) {
        sum += arr[i];
    }
}

// 编译器自动展开
void sum_auto(int* arr, int n) {
    int sum = 0;
    #pragma GCC unroll 4
    for (int i = 0; i < n; i++) {
        sum += arr[i];
    }
}

或使用编译选项:
g++ -O3 -funroll-loops program.cpp

验证展开效果:
g++ -O3 -S -masm=intel program.cpp
# 查看生成的汇编代码
```

#### 8.2.2 循环融合和分裂

```cpp
// 反例: 多次遍历
void separate_loops(int* a, int* b, int n) {
    for (int i = 0; i < n; i++) {
        a[i] = a[i] * 2;
    }

    for (int i = 0; i < n; i++) {
        b[i] = b[i] + 1;
    }
}

// 优化: 循环融合
void fused_loop(int* a, int* b, int n) {
    for (int i = 0; i < n; i++) {
        a[i] = a[i] * 2;
        b[i] = b[i] + 1;
    }
}

// 但有时需要分裂
void bad_fusion(int* arr, int n) {
    for (int i = 0; i < n; i++) {
        arr[i] = expensive_compute(arr[i]);  // 计算密集
        arr[i] = cheap_operation(arr[i]);    // 简单操作
    }
}

// 分裂以利于向量化
void split_loop(int* arr, int n) {
    // 第一个循环可以向量化
    for (int i = 0; i < n; i++) {
        arr[i] = expensive_compute(arr[i]);
    }

    // 第二个循环也可以向量化
    for (int i = 0; i < n; i++) {
        arr[i] = cheap_operation(arr[i]);
    }
}
```

### 8.3 内联优化

```cpp
// 小函数适合内联
inline int add(int a, int b) {
    return a + b;
}

// 强制内联
__attribute__((always_inline))
inline int must_inline(int x) {
    return x * x;
}

// 禁止内联 (用于分析)
__attribute__((noinline))
int no_inline(int x) {
    return x * x;
}

// 查看内联决策
g++ -O3 -Winline program.cpp
# 警告: 建议内联但未内联的函数

// 控制内联限制
g++ -O3 -finline-limit=600 program.cpp
# 增加内联阈值

内联权衡:
优点:
• 消除函数调用开销
• 暴露更多优化机会
• 提高指令局部性

缺点:
• 增加代码大小
• 可能降低指令缓存命中率
• 增加编译时间

经验法则:
• 函数 < 10行: 适合内联
• 热路径上的小函数: 内联
• 大函数、递归函数: 不内联
```

---

## 9. SIMD向量化优化

### 9.1 理解SIMD

```
SIMD (Single Instruction Multiple Data)
单条指令处理多个数据

标量处理 (Scalar):
┌───┐  ┌───┐  ┌───┐
│ + │  │ + │  │ + │  ... 4条指令
└───┘  └───┘  └───┘
  ↓      ↓      ↓
  c0     c1     c2

SIMD处理:
┌─────────────────┐
│   Vector ADD    │  1条指令!
└─────────────────┘
  ↓   ↓   ↓   ↓
  c0  c1  c2  c3

x86-64 SIMD指令集:
• SSE (128-bit): 4 x float / 2 x double
• AVX2 (256-bit): 8 x float / 4 x double
• AVX-512 (512-bit): 16 x float / 8 x double

ARM SIMD:
• NEON (128-bit): 4 x float / 2 x double
• SVE (可伸缩): 128-2048 bits
```

### 9.2 自动向量化

```cpp
// 简单循环 - 编译器可以自动向量化
void add_arrays(float* a, float* b, float* c, int n) {
    for (int i = 0; i < n; i++) {
        c[i] = a[i] + b[i];
    }
}

编译并查看向量化:
g++ -O3 -march=native -fopt-info-vec-optimized program.cpp

输出:
program.cpp:2:5: optimized: loop vectorized using 32 byte vectors

查看汇编验证:
g++ -O3 -march=native -S -masm=intel program.cpp
less program.s

# 应该看到 AVX指令:
vaddps ymm0, ymm1, ymm2  # AVX 256-bit 向量加法

// 妨碍向量化的因素

// 1. 数据依赖
void bad_dependency(float* arr, int n) {
    for (int i = 1; i < n; i++) {
        arr[i] = arr[i] + arr[i-1];  // 依赖前一个元素
    }
}

// 2. 函数调用
void bad_function_call(float* arr, int n) {
    for (int i = 0; i < n; i++) {
        arr[i] = sqrt(arr[i]);  // 函数调用可能阻止向量化
    }
}

// 3. 条件分支
void bad_branch(float* arr, int n) {
    for (int i = 0; i < n; i++) {
        if (arr[i] > 0) {  // 分支
            arr[i] = arr[i] * 2;
        }
    }
}

// 帮助编译器向量化

// 1. 使用restrict指针 (告诉编译器无别名)
void add_arrays_restrict(
    float* __restrict__ a,
    float* __restrict__ b,
    float* __restrict__ c,
    int n) {
    for (int i = 0; i < n; i++) {
        c[i] = a[i] + b[i];  // 编译器知道a, b, c不重叠
    }
}

// 2. 对齐数据
alignas(32) float a[1024];  // AVX需要32字节对齐
alignas(32) float b[1024];
alignas(32) float c[1024];

// 3. 编译指示
void add_with_pragma(float* a, float* b, float* c, int n) {
    #pragma GCC ivdep  // 忽略依赖
    for (int i = 0; i < n; i++) {
        c[i] = a[i] + b[i];
    }
}

// 4. OpenMP SIMD
void add_with_omp(float* a, float* b, float* c, int n) {
    #pragma omp simd
    for (int i = 0; i < n; i++) {
        c[i] = a[i] + b[i];
    }
}
```

### 9.3 手动SIMD (Intrinsics)

```cpp
#include <immintrin.h>  // AVX

// 标量版本
void add_scalar(float* a, float* b, float* c, int n) {
    for (int i = 0; i < n; i++) {
        c[i] = a[i] + b[i];
    }
}

// AVX intrinsics版本
void add_avx(float* a, float* b, float* c, int n) {
    int i;

    // 处理8个一组 (256-bit / 32-bit = 8)
    for (i = 0; i + 7 < n; i += 8) {
        __m256 va = _mm256_load_ps(&a[i]);   // 加载8个float
        __m256 vb = _mm256_load_ps(&b[i]);
        __m256 vc = _mm256_add_ps(va, vb);   // 向量加法
        _mm256_store_ps(&c[i], vc);          // 存储结果
    }

    // 处理剩余元素
    for (; i < n; i++) {
        c[i] = a[i] + b[i];
    }
}

// 更复杂的例子: 点积
float dot_product_avx(float* a, float* b, int n) {
    __m256 sum_vec = _mm256_setzero_ps();
    int i;

    for (i = 0; i + 7 < n; i += 8) {
        __m256 va = _mm256_load_ps(&a[i]);
        __m256 vb = _mm256_load_ps(&b[i]);
        __m256 prod = _mm256_mul_ps(va, vb);
        sum_vec = _mm256_add_ps(sum_vec, prod);
    }

    // 水平求和
    float sum_array[8];
    _mm256_store_ps(sum_array, sum_vec);
    float sum = 0;
    for (int j = 0; j < 8; j++) {
        sum += sum_array[j];
    }

    // 剩余元素
    for (; i < n; i++) {
        sum += a[i] * b[i];
    }

    return sum;
}

性能测试:
perf stat -e cycles,instructions ./scalar_version
perf stat -e cycles,instructions ./avx_version

典型结果:
标量版本: 8.0 cycles/element
AVX版本:  1.2 cycles/element
加速比:   6.7x (理论8x, 实际略低)
```

### 9.4 SIMD优化技巧

```cpp
// 1. 数据对齐
void* aligned_alloc_wrapper(size_t size, size_t alignment) {
    void* ptr;
    posix_memalign(&ptr, alignment, size);
    return ptr;
}

float* a = (float*)aligned_alloc_wrapper(n * sizeof(float), 32);

// 2. 预取
void process_with_prefetch_simd(float* data, int n) {
    for (int i = 0; i < n; i += 8) {
        // 预取未来数据
        _mm_prefetch((char*)&data[i + 64], _MM_HINT_T0);

        // SIMD处理
        __m256 v = _mm256_load_ps(&data[i]);
        v = _mm256_mul_ps(v, v);
        _mm256_store_ps(&data[i], v);
    }
}

// 3. 混合标量和向量代码
void hybrid_process(float* data, int n) {
    int i;

    // SIMD快速路径
    for (i = 0; i + 7 < n; i += 8) {
        __m256 v = _mm256_load_ps(&data[i]);
        // ... SIMD处理
        _mm256_store_ps(&data[i], v);
    }

    // 标量处理剩余
    for (; i < n; i++) {
        data[i] = process_scalar(data[i]);
    }
}

// 4. 使用FMA (Fused Multiply-Add)
void fma_example(float* a, float* b, float* c, int n) {
    for (int i = 0; i + 7 < n; i += 8) {
        __m256 va = _mm256_load_ps(&a[i]);
        __m256 vb = _mm256_load_ps(&b[i]);
        __m256 vc = _mm256_load_ps(&c[i]);

        // c = a * b + c (1条指令)
        vc = _mm256_fmadd_ps(va, vb, vc);

        _mm256_store_ps(&c[i], vc);
    }
}

编译选项:
g++ -O3 -march=native -mfma program.cpp
```

---

## 10. 微架构级优化

### 10.1 理解现代CPU微架构

```
Intel/AMD CPU 流水线 (简化):

┌──────────────────────────────────────────────────────┐
│                   Frontend                           │
│  ┌────────┐  ┌─────────┐  ┌─────────┐              │
│  │ Fetch  │→ │ Decode  │→ │  Rename │              │
│  └────────┘  └─────────┘  └─────────┘              │
└──────────────────────────────────────────────────────┘
                      ↓
┌──────────────────────────────────────────────────────┐
│               Reorder Buffer (ROB)                   │
└──────────────────────────────────────────────────────┘
                      ↓
┌──────────────────────────────────────────────────────┐
│                   Backend                            │
│  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐           │
│  │ ALU0 │  │ ALU1 │  │ Load │  │Store │  ...      │
│  └──────┘  └──────┘  └──────┘  └──────┘           │
└──────────────────────────────────────────────────────┘
                      ↓
┌──────────────────────────────────────────────────────┐
│                  Retire                              │
└──────────────────────────────────────────────────────┘

关键概念:

1. 超标量 (Superscalar):
   每周期可以发射多条指令 (4-6条)

2. 乱序执行 (Out-of-Order):
   指令不按程序顺序执行，避免停顿

3. 推测执行 (Speculative):
   预测分支，提前执行指令

4. 寄存器重命名:
   消除WAR/WAW依赖

性能瓶颈:
• Frontend: 指令获取/解码带宽
• Backend: 执行单元/内存带宽
• ROB大小: 限制并行窗口
```

### 10.2 延迟和吞吐量

```cpp
// 理解指令延迟和吞吐量

指令延迟 (Latency): 指令完成所需的周期数
吞吐量 (Throughput): 单位时间可以发射的指令数

示例 (Skylake):
┌──────────────┬─────────┬──────────┬────────┐
│  指令         │ 延迟    │ 吞吐量   │ 端口   │
├──────────────┼─────────┼──────────┼────────┤
│ ADD/SUB      │  1      │   4/cycle│ 0156   │
│ IMUL(32-bit) │  3      │   1/cycle│ 1      │
│ IMUL(64-bit) │  3-4    │   1/cycle│ 1      │
│ IDIV(64-bit) │ 42-95   │ 1/42cycle│ 0      │
│ FP ADD       │  4      │   2/cycle│ 01     │
│ FP MUL       │  4      │   2/cycle│ 01     │
│ FP DIV       │  14     │  1/4cycle│ 0      │
│ SQRT         │  15     │  1/4cycle│ 0      │
│ Load         │  5      │   2/cycle│ 23     │
│ Store        │  1      │   1/cycle│ 4,789  │
└──────────────┴─────────┴──────────┴────────┘

优化策略:

// 反例: 依赖链
int slow_multiply(int n) {
    int result = 1;
    for (int i = 0; i < n; i++) {
        result = result * 2;  // 串行依赖
    }
    return result;
}
// 延迟: n * 3 cycles

// 优化: 打破依赖
int fast_multiply(int n) {
    return 1 << n;  // 1条移位指令, 1 cycle
}

// 反例: 除法密集
float bad_average(float* data, int n) {
    for (int i = 0; i < n; i++) {
        data[i] = data[i] / n;  // 除法延迟14 cycles
    }
}

// 优化: 提取公因子
float good_average(float* data, int n) {
    float inv_n = 1.0f / n;  // 1次除法
    for (int i = 0; i < n; i++) {
        data[i] = data[i] * inv_n;  // 乘法延迟4 cycles
    }
}
```

### 10.3 端口压力分析

```bash
# Intel VTune 端口利用率分析
vtune -collect uarch-exploration ./program
vtune -report hotspots -r uarch_results

# 或使用perf (需要内核支持)
perf stat -e \
  uops_dispatched_port.port_0,\
  uops_dispatched_port.port_1,\
  uops_dispatched_port.port_2,\
  uops_dispatched_port.port_3,\
  uops_dispatched_port.port_4,\
  uops_dispatched_port.port_5,\
  uops_dispatched_port.port_6,\
  uops_dispatched_port.port_7 \
  ./program

# 输出示例:
   1,234,567,890  uops_dispatched_port.port_0  # ALU, FP
     678,901,234  uops_dispatched_port.port_1  # ALU, FP
   2,345,678,901  uops_dispatched_port.port_2  # Load
   2,123,456,789  uops_dispatched_port.port_3  # Load
   1,890,123,456  uops_dispatched_port.port_4  # Store
     567,890,123  uops_dispatched_port.port_5  # ALU
     234,567,890  uops_dispatched_port.port_6  # Branch
     890,123,456  uops_dispatched_port.port_7  # Store Addr

分析:
• Port 2, 3 压力大 → Memory Load bound
• Port 0, 1, 5 压力均衡 → 计算负载均衡
• Port 4, 7 压力大 → Store bound
```

### 10.4 减少数据依赖

```cpp
// 反例: 长依赖链
int sum_with_dependency(int* arr, int n) {
    int sum = 0;
    for (int i = 0; i < n; i++) {
        sum = sum + arr[i];  // 每次加法依赖上次结果
    }
    return sum;
}

// 优化: 多累加器 (Tree Reduction)
int sum_parallel(int* arr, int n) {
    int sum0 = 0, sum1 = 0, sum2 = 0, sum3 = 0;
    int i;

    for (i = 0; i + 3 < n; i += 4) {
        sum0 += arr[i];
        sum1 += arr[i+1];
        sum2 += arr[i+2];
        sum3 += arr[i+3];
    }

    // 树形归约
    sum0 += sum1;
    sum2 += sum3;
    int sum = sum0 + sum2;

    for (; i < n; i++) {
        sum += arr[i];
    }

    return sum;
}

原理:
• 单累加器: 依赖链长度 = n
• 4累加器: 依赖链长度 = n/4 + log2(4)
• 提高ILP (Instruction Level Parallelism)

验证:
perf stat -e cycles,instructions ./single_accumulator
perf stat -e cycles,instructions ./multi_accumulator

# IPC应该显著提升
```

---

## 11. 实战：完整的性能优化流程

### 案例: 优化矩阵乘法

```cpp
// 第0步: 基线版本 (Baseline)
void matmul_baseline(float* A, float* B, float* C, int N) {
    for (int i = 0; i < N; i++) {
        for (int j = 0; j < N; j++) {
            float sum = 0.0f;
            for (int k = 0; k < N; k++) {
                sum += A[i*N + k] * B[k*N + j];
            }
            C[i*N + j] = sum;
        }
    }
}

// 测试性能
void benchmark(const char* name,
               void (*func)(float*, float*, float*, int),
               int N) {
    float* A = new float[N*N];
    float* B = new float[N*N];
    float* C = new float[N*N];

    // 初始化
    for (int i = 0; i < N*N; i++) {
        A[i] = (float)rand() / RAND_MAX;
        B[i] = (float)rand() / RAND_MAX;
    }

    auto start = std::chrono::high_resolution_clock::now();
    func(A, B, C, N);
    auto end = std::chrono::high_resolution_clock::now();

    double time = std::chrono::duration<double>(end - start).count();
    double gflops = (2.0 * N * N * N) / time / 1e9;

    printf("%20s: %.3f s, %.2f GFLOPS\n", name, time, gflops);

    delete[] A; delete[] B; delete[] C;
}

int main() {
    int N = 1024;
    benchmark("Baseline", matmul_baseline, N);
}

// 输出:
//            Baseline: 15.234 s, 0.14 GFLOPS
```

```bash
# 第1步: 性能剖析 - 找到热点

perf record -g ./matmul
perf report

# 输出:
# 99.8%  matmul  matmul  [.] matmul_baseline
#   ↓
#   100% 在内层循环

perf stat -e cycles,instructions,cache-references,cache-misses \
  ./matmul

# 输出:
#   cycles:          45,678,901,234
#   instructions:    23,456,789,012  (IPC = 0.51)
#   cache-references: 8,901,234,567
#   cache-misses:     2,345,678,901  (26.3% miss rate) ⚠️

# 诊断: 严重的缓存miss问题 + 低IPC
```

```cpp
// 第2步: 优化1 - 改进缓存局部性 (Loop Interchange)

void matmul_reorder(float* A, float* B, float* C, int N) {
    // 调整循环顺序: i-j-k → i-k-j
    for (int i = 0; i < N; i++) {
        for (int k = 0; k < N; k++) {  // k和j交换
            float a = A[i*N + k];
            for (int j = 0; j < N; j++) {
                C[i*N + j] += a * B[k*N + j];  // 顺序访问B和C
            }
        }
    }
}

benchmark("Reorder", matmul_reorder, N);

// 输出:
//              Reorder: 8.567 s, 0.25 GFLOPS (1.78x faster)

perf stat -e cache-misses ./matmul_reorder
# cache-misses: 567,890,123 (6.4% miss rate) ✓ 改善显著
```

```cpp
// 第3步: 优化2 - 分块 (Blocking/Tiling) 利用L1缓存

void matmul_blocked(float* A, float* B, float* C, int N) {
    const int BLOCK = 32;  // 调整到L1缓存大小

    // 初始化C
    for (int i = 0; i < N*N; i++) C[i] = 0;

    for (int ii = 0; ii < N; ii += BLOCK) {
        for (int jj = 0; jj < N; jj += BLOCK) {
            for (int kk = 0; kk < N; kk += BLOCK) {
                // 块内计算
                int i_max = std::min(ii + BLOCK, N);
                int j_max = std::min(jj + BLOCK, N);
                int k_max = std::min(kk + BLOCK, N);

                for (int i = ii; i < i_max; i++) {
                    for (int k = kk; k < k_max; k++) {
                        float a = A[i*N + k];
                        for (int j = jj; j < j_max; j++) {
                            C[i*N + j] += a * B[k*N + j];
                        }
                    }
                }
            }
        }
    }
}

benchmark("Blocked", matmul_blocked, N);

// 输出:
//              Blocked: 2.345 s, 0.91 GFLOPS (6.5x faster)

perf stat -e L1-dcache-load-misses,LLC-load-misses ./matmul_blocked
# L1-misses: 123,456,789 (1.4% miss rate) ✓
# LLC-misses: 12,345,678 (0.1% miss rate) ✓
```

```cpp
// 第4步: 优化3 - SIMD向量化

#include <immintrin.h>

void matmul_simd(float* A, float* B, float* C, int N) {
    const int BLOCK = 32;

    for (int i = 0; i < N*N; i++) C[i] = 0;

    for (int ii = 0; ii < N; ii += BLOCK) {
        for (int jj = 0; jj < N; jj += BLOCK) {
            for (int kk = 0; kk < N; kk += BLOCK) {
                int i_max = std::min(ii + BLOCK, N);
                int j_max = std::min(jj + BLOCK, N);
                int k_max = std::min(kk + BLOCK, N);

                for (int i = ii; i < i_max; i++) {
                    for (int k = kk; k < k_max; k++) {
                        __m256 a_vec = _mm256_set1_ps(A[i*N + k]);

                        int j;
                        for (j = jj; j + 7 < j_max; j += 8) {
                            __m256 b_vec = _mm256_loadu_ps(&B[k*N + j]);
                            __m256 c_vec = _mm256_loadu_ps(&C[i*N + j]);
                            c_vec = _mm256_fmadd_ps(a_vec, b_vec, c_vec);
                            _mm256_storeu_ps(&C[i*N + j], c_vec);
                        }

                        // 处理剩余
                        for (; j < j_max; j++) {
                            C[i*N + j] += A[i*N + k] * B[k*N + j];
                        }
                    }
                }
            }
        }
    }
}

benchmark("SIMD", matmul_simd, N);

// 输出:
//                 SIMD: 0.567 s, 3.77 GFLOPS (26.9x faster)
```

```cpp
// 第5步: 优化4 - 多线程并行

#include <omp.h>

void matmul_parallel(float* A, float* B, float* C, int N) {
    const int BLOCK = 32;

    for (int i = 0; i < N*N; i++) C[i] = 0;

    #pragma omp parallel for collapse(2)
    for (int ii = 0; ii < N; ii += BLOCK) {
        for (int jj = 0; jj < N; jj += BLOCK) {
            for (int kk = 0; kk < N; kk += BLOCK) {
                int i_max = std::min(ii + BLOCK, N);
                int j_max = std::min(jj + BLOCK, N);
                int k_max = std::min(kk + BLOCK, N);

                for (int i = ii; i < i_max; i++) {
                    for (int k = kk; k < k_max; k++) {
                        __m256 a_vec = _mm256_set1_ps(A[i*N + k]);

                        int j;
                        for (j = jj; j + 7 < j_max; j += 8) {
                            __m256 b_vec = _mm256_loadu_ps(&B[k*N + j]);
                            __m256 c_vec = _mm256_loadu_ps(&C[i*N + j]);
                            c_vec = _mm256_fmadd_ps(a_vec, b_vec, c_vec);
                            _mm256_storeu_ps(&C[i*N + j], c_vec);
                        }

                        for (; j < j_max; j++) {
                            C[i*N + j] += A[i*N + k] * B[k*N + j];
                        }
                    }
                }
            }
        }
    }
}

// 编译: g++ -O3 -march=native -fopenmp matmul.cpp -o matmul

OMP_NUM_THREADS=8 ./matmul

benchmark("Parallel", matmul_parallel, N);

// 输出 (8核CPU):
//            Parallel: 0.089 s, 24.02 GFLOPS (171x faster)
```

### 优化总结

```
优化阶段            时间(s)   GFLOPS  加速比  关键改进
──────────────────────────────────────────────────────
Baseline           15.234    0.14    1.00x   -
Loop Reorder        8.567    0.25    1.78x   缓存局部性
Blocking            2.345    0.91    6.50x   L1缓存利用
SIMD               0.567    3.77   26.87x   向量化
Parallel (8核)     0.089   24.02  171.19x   多线程

关键性能指标变化:
指标                Baseline → Parallel
──────────────────────────────────────
Cache Miss Rate      26.3%  →   0.8%
IPC                  0.51   →   2.34
CPU利用率 (8核)      12.5%  →   95.2%

学到的教训:
1. 缓存优化是第一优先级 (6.5x)
2. SIMD向量化效果显著 (4x)
3. 多线程扩展性良好 (6.4x on 8 cores)
4. 组合优化效果叠加
```

---

## 12. 性能优化的反模式

### 12.1 过早优化

```cpp
// 反模式: 牺牲可读性进行微优化

// 不好: 过度优化，难以维护
int obscure_average(int* arr, int n) {
    register int sum asm("rax") = 0;  // 强制使用特定寄存器
    int i = 0;

    // 手动展开16路
    for (; i + 15 < n; i += 16) {
        sum += arr[i] + arr[i+1] + arr[i+2] + arr[i+3] +
               arr[i+4] + arr[i+5] + arr[i+6] + arr[i+7] +
               arr[i+8] + arr[i+9] + arr[i+10] + arr[i+11] +
               arr[i+12] + arr[i+13] + arr[i+14] + arr[i+15];
    }

    for (; i < n; i++) sum += arr[i];

    return (sum >> 31) | (sum / n);  // 奇怪的除法优化
}

// 好: 简洁，让编译器优化
int clear_average(int* arr, int n) {
    int sum = 0;
    for (int i = 0; i < n; i++) {
        sum += arr[i];
    }
    return sum / n;
}

// 使用-O3编译，性能通常相同或更好！

原则:
1. 先写清晰的代码
2. 测量发现瓶颈
3. 在热点处优化
4. 保持其他代码简洁
```

### 12.2 忽略Amdahl定律

```
// 错误: 优化非瓶颈部分

// 假设程序执行时间分布:
// 函数A: 1%   (0.1s)
// 函数B: 94%  (9.4s)
// 函数C: 5%   (0.5s)

// 错误做法: 花大量时间优化函数A
// 即使优化10倍，总提升: 10s → 9.91s (0.9%)

// 正确做法: 优化函数B
// 优化2倍，总提升: 10s → 5.8s (42%)

教训:
使用profiling找到真正的瓶颈！
不要靠直觉猜测！
```

### 12.3 盲目使用技巧

```cpp
// 反模式: 不测量就应用"优化"

// 错误: 认为位运算总是更快
int bad_multiply_by_10(int x) {
    return (x << 3) + (x << 1);  // x * 8 + x * 2
}

int good_multiply_by_10(int x) {
    return x * 10;  // 编译器会优化！
}

// 现代编译器生成的汇编可能完全相同

// 错误: 认为switch总比if-else快
int bad_small_values(int x) {
    switch (x) {  // 对于小范围，if可能更快
        case 0: return 1;
        case 1: return 2;
        case 2: return 4;
        default: return 0;
    }
}

int good_small_values(int x) {
    if (x == 0) return 1;
    if (x == 1) return 2;
    if (x == 2) return 4;
    return 0;
}

原则:
• 不要假设，要测量
• 编译器很聪明
• 不同场景最优解不同
```

### 12.4 忽略编译器优化

```bash
# 反模式: 花时间手动优化编译器能做的事

# 错误流程:
1. 写复杂的手动优化代码
2. 使用 -O0 编译
3. 发现性能不好
4. 继续手动优化...

# 正确流程:
1. 写清晰的代码
2. 使用 -O2/-O3 编译
3. 测量性能
4. 如果不够快，用profiling找瓶颈
5. 只优化真正的瓶颈

示例:
gcc -O0 program.cpp  # 10.5s
gcc -O2 program.cpp  # 2.3s   (4.6x faster, 免费!)
gcc -O3 program.cpp  # 1.8s   (5.8x faster)

+ 手动优化热点    # 0.5s   (21x faster)
```

---

## 附录A: 性能分析速查表

### 常用perf命令

```bash
# 快速热点分析
perf top

# 记录性能数据
perf record -g ./program

# 查看报告
perf report

# 统计硬件事件
perf stat ./program

# 详细事件
perf stat -e cycles,instructions,cache-misses,branch-misses ./program

# CPU使用率分析
perf stat -e cpu-clock,task-clock ./program

# 缓存分析
perf stat -e L1-dcache-loads,L1-dcache-load-misses,LLC-loads,LLC-load-misses ./program

# 分支预测
perf stat -e branches,branch-misses ./program

# 查看可用事件
perf list

# 针对特定CPU
perf stat -C 0 ./program

# 针对特定进程
perf stat -p <PID>

# 记录系统级
perf record -a -g
```

### 性能指标判断标准

```
指标                     优秀     良好     需优化    问题严重
─────────────────────────────────────────────────────────
IPC                     > 2.0   1.5-2.0   1.0-1.5    < 1.0
L1 Cache Miss           < 2%    2-5%      5-10%      > 10%
LLC Cache Miss          < 1%    1-5%      5-15%      > 15%
Branch Miss             < 1%    1-5%      5-10%      > 10%
TLB Miss                < 0.1%  0.1-0.5%  0.5-1%     > 1%
CPU Utilization (多核)  > 90%   70-90%    50-70%     < 50%
Context Switches/sec    < 1000  1000-5000 5000-10000 > 10000
```

### GCC/Clang编译选项速查

```bash
# 优化级别
-O0          # 无优化（调试）
-O1 / -O    # 基础优化
-O2          # 推荐优化（生产）
-O3          # 激进优化
-Ofast       # 最快（可能不符合标准）
-Os          # 优化大小
-Og          # 优化调试体验

# 架构优化
-march=native      # 针对本机CPU
-march=skylake     # 针对Skylake
-march=znver2      # 针对AMD Zen 2
-mtune=native      # 调优指令选择

# SIMD
-msse4.2          # 启用SSE4.2
-mavx2            # 启用AVX2
-mavx512f         # 启用AVX-512
-mfma             # 启用FMA

# 循环优化
-funroll-loops    # 循环展开
-ftree-vectorize  # 自动向量化
-ffast-math       # 快速数学（不安全）

# 内联
-finline-limit=N  # 内联大小限制

# LTO
-flto             # 链接时优化

# PGO
-fprofile-generate  # 生成profile
-fprofile-use       # 使用profile

# 调试优化
-fopt-info-vec    # 向量化信息
-fopt-info-loop   # 循环优化信息
-Rpass=.*         # LLVM优化报告
```

---

## 附录B: 推荐阅读和资源

### 书籍

1. **Computer Architecture: A Quantitative Approach**
   - Hennessy & Patterson
   - 深入理解计算机体系结构

2. **Software Optimization for High-Performance Computing**
   - 实用优化技术

3. **The Art of Writing Efficient Programs**
   - 现代C++性能编程

### 在线资源

1. **Agner Fog's Optimization Manuals**
   - https://www.agner.org/optimize/
   - 微架构优化圣经

2. **Intel Optimization Reference Manual**
   - https://software.intel.com/content/www/us/en/develop/documentation/

3. **Performance Matters (Video)**
   - Emery Berger
   - 性能分析方法论

4. **Brendan Gregg's Blog**
   - https://www.brendangregg.com/
   - 系统性能分析

### 工具文档

1. **perf Wiki**
   - https://perf.wiki.kernel.org/

2. **VTune User Guide**
   - Intel VTune Profiler文档

3. **LIKWID**
   - 轻量级性能工具
   - https://github.com/RRZE-HPC/likwid

---

## 总结

### 性能优化的核心原则

1. **测量，不要猜测**
   - 使用profiling工具
   - 找到真正的瓶颈

2. **理解硬件**
   - CPU流水线
   - 缓存层次
   - 内存带宽

3. **从上到下优化**
   - 算法 > 数据结构 > 代码 > 编译器

4. **验证每次优化**
   - 对比前后性能
   - 确保正确性

5. **保持简洁**
   - 不要过度优化
   - 可读性也很重要

### 优化检查清单

```
□ 使用了正确的算法（O(n) vs O(n²)）
□ 使用了合适的数据结构
□ 开启了编译器优化（-O2/-O3）
□ 运行了profiler找到热点
□ 检查了缓存命中率
□ 检查了分支预测
□ 考虑了SIMD向量化
□ 利用了多线程并行
□ 减少了系统调用
□ 测试了不同输入规模
□ 验证了优化效果
□ 代码仍然可维护
```

### 最后的建议

**性能优化是一个迭代过程：**

```
测量 → 分析 → 优化 → 验证 → 重复
```

**记住：**
- 最大的优化往往来自算法改进
- 编译器优化是免费的午餐
- 过早优化是万恶之源
- 但适时优化是必要的
- 保持好奇心，持续学习

**祝你在性能优化的道路上取得成功！**
