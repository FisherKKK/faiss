# 比较与排序算法的底层优化深度剖析

## 概述

本文深入剖析Faiss中比较、排序和哈希表的底层实现，特别关注CMax/CMin比较模板、并行归并排序、桶排序和并行哈希表的C++优化细节。这些底层组件是向量检索系统中Top-K搜索、倒排列表处理和结果聚合的核心基础设施。

---

## 目录

1. [比较器模板设计](#1-比较器模板设计)
2. [并行归并排序](#2-并行归并排序)
3. [桶排序优化](#3-桶排序优化)
4. [原地桶排序](#4-原地桶排序)
5. [并行哈希表](#5-并行哈希表)
6. [距离计算器集成](#6-距离计算器集成)

---

## 1. 比较器模板设计

### 1.1 C对象设计理念

Faiss使用统一的`C`对象处理最小堆和最大堆，实现降序和升序序列的统一处理 (faiss/utils/ordered_key_value.h)。

**设计目标**: 对于给定阈值`threshold`，保留满足以下条件的值`x`：

```
C::cmp(threshold, x) == true
```

### 1.2 CMin实现

**最小堆** = 堆顶存储最小值 → 用于查找数组的**最大值** (ordered_key_value.h:42-62):

```cpp
template <typename T_, typename TI_>
struct CMin {
    typedef T_ T;
    typedef TI_ TI;
    typedef CMax<T_, TI_> Crev; // 反向比较引用

    // 单值比较: a < b 时返回true
    inline static bool cmp(T a, T b) {
        return a < b;
    }

    // 双值比较: 处理相等时的平局决胜
    inline static bool cmp2(T a1, T b1, TI a2, TI b2) {
        return (a1 < b1) || ((a1 == b1) && (a2 < b2));
    }

    // 中性元素: 最小可能值
    inline static T neutral() {
        return std::numeric_limits<T>::lowest();
    }

    static const bool is_max = false;

    // 获取下一个可表示值 (用于精度处理)
    inline static T nextafter(T x) {
        return cmin_nextafter(x);
    }
};
```

**使用场景**:
- **L2距离** (最小化): 使用`CMin`堆，堆顶保存当前最大的距离
- **Top-K搜索**: 当新距离`< bh_val[0]`时替换堆顶

### 1.3 CMax实现

**最大堆** = 堆顶存储最大值 → 用于查找数组的**最小值** (ordered_key_value.h:65-84):

```cpp
template <typename T_, typename TI_>
struct CMax {
    typedef T_ T;
    typedef TI_ TI;
    typedef CMin<T_, TI_> Crev;

    // 单值比较: a > b 时返回true
    inline static bool cmp(T a, T b) {
        return a > b;
    }

    // 双值比较
    inline static bool cmp2(T a1, T b1, TI a2, TI b2) {
        return (a1 > b1) || ((a1 == b1) && (a2 > b2));
    }

    // 中性元素: 最大可能值
    inline static T neutral() {
        return std::numeric_limits<T>::max();
    }

    static const bool is_max = true;

    inline static T nextafter(T x) {
        return cmax_nextafter(x);
    }
};
```

**使用场景**:
- **内积相似度** (最大化): 使用`CMax`堆，堆顶保存当前最小的相似度

### 1.4 Nextafter函数

**精度处理**: 用于获取下一个可表示的浮点数 (ordered_key_value.h:86-104):

```cpp
// float类型: 使用标准库函数
template <>
inline float cmin_nextafter<float>(float x) {
    return std::nextafterf(x, -HUGE_VALF);  // 向负无穷方向
}

template <>
inline float cmax_nextafter<float>(float x) {
    return std::nextafterf(x, HUGE_VALF);   // 向正无穷方向
}

// uint16_t类型: 简单加减
template <>
inline uint16_t cmin_nextafter<uint16_t>(uint16_t x) {
    return x - 1;
}

template <>
inline uint16_t cmax_nextafter<uint16_t>(uint16_t x) {
    return x + 1;
}
```

**应用场景**: 在距离计算中处理边界情况，确保严格的比较语义。

### 1.5 VectorDistance集成

**距离计算器模板** (extra_distances-inl.h:21-35):

```cpp
template <MetricType mt>
struct VectorDistance {
    size_t d;
    float metric_arg;
    static constexpr MetricType metric = mt;
    static constexpr bool is_similarity = is_similarity_metric(mt);

    inline float operator()(const float* x, const float* y) const;

    // 根据度量类型选择堆类型
    using C = typename std::conditional<
            is_similarity_metric(mt),      // 相似度度量 -> 最大堆
            CMin<float, int64_t>,          // 距离度量 -> 最小堆
            CMax<float, int64_t>>::type;
};
```

**编译期类型选择**:

```cpp
// L2距离 (is_similarity=false) -> CMax (最小堆)
VectorDistance<METRIC_L2>::C = CMax<float, int64_t>

// 内积 (is_similarity=true) -> CMin (最大堆)
VectorDistance<METRIC_INNER_PRODUCT>::C = CMin<float, int64_t>
```

---

## 2. 并行归并排序

### 2.1 Argsort基础实现

**串行版本** (sorting.cpp:128-134):

```cpp
void fvec_argsort(size_t n, const float* vals, size_t* perm) {
    // 初始化排列 [0, 1, 2, ..., n-1]
    for (size_t i = 0; i < n; i++) {
        perm[i] = i;
    }
    ArgsortComparator comp = {vals};
    std::sort(perm, perm + n, comp);
}

namespace {
struct ArgsortComparator {
    const float* vals;
    bool operator()(const size_t a, const size_t b) const {
        return vals[a] < vals[b];
    }
};
}
```

### 2.2 并行归并排序算法

**核心思想**:
1. 数据分片：将数组分成nt个片段
2. 独立排序：每个线程排序自己的片段
3. 归并树：树状归并相邻片段

**并行实现** (sorting.cpp:136-201):

```cpp
void fvec_argsort_parallel(size_t n, const float* vals, size_t* perm) {
    size_t* perm2 = new size_t[n];
    size_t *permB = perm2, *permA = perm;  // 交替缓冲区

    int nt = omp_get_max_threads();

    // 确保最终结果在perm中
    {
        int nseg = nt;
        while (nseg > 1) {
            nseg = (nseg + 1) / 2;
            std::swap(permA, permB);
        }
    }

    // 阶段1: 初始化排列并独立排序
#pragma omp parallel
    for (size_t i = 0; i < n; i++) {
        permA[i] = i;
    }

    ArgsortComparator comp = {vals};
    std::vector<SegmentS> segs(nt);

    // 每个线程排序自己的片段
#pragma omp parallel for
    for (int t = 0; t < nt; t++) {
        size_t i0 = t * n / nt;
        size_t i1 = (t + 1) * n / nt;
        SegmentS seg = {i0, i1};
        std::sort(permA + seg.i0, permA + seg.i1, comp);
        segs[t] = seg;
    }

    // 阶段2: 归并相邻片段
    int prev_nested = omp_get_nested();
    omp_set_nested(1);

    int nseg = nt;
    while (nseg > 1) {
        int nseg1 = (nseg + 1) / 2;
        int sub_nt = nseg % 2 == 0 ? nt : nt - 1;
        int sub_nseg1 = nseg / 2;

#pragma omp parallel for num_threads(nseg1)
        for (int s = 0; s < nseg; s += 2) {
            if (s + 1 == nseg) {
                // 孤立片段，直接复制
                memcpy(permB + segs[s].i0,
                       permA + segs[s].i0,
                       segs[s].len() * sizeof(size_t));
            } else {
                // 归并两个相邻片段
                int t0 = s * sub_nt / sub_nseg1;
                int t1 = (s + 1) * sub_nt / sub_nseg1;
                parallel_merge(
                        permA, permB, segs[s], segs[s + 1], t1 - t0, comp);
            }
        }

        // 更新片段列表
        for (int s = 0; s < nseg; s += 2) {
            segs[s / 2] = segs[s];
        }
        nseg = nseg1;
        std::swap(permA, permB);
    }

    omp_set_nested(prev_nested);
    delete[] perm2;
}
```

### 2.3 并行归并核心算法

**Parallel Merge实现** (sorting.cpp:46-124):

```cpp
template <typename T>
void parallel_merge(
        const T* src,
        T* dst,
        SegmentS& s1,
        SegmentS& s2,
        int nt,                      // 并行线程数
        const ArgsortComparator& comp) {
    // 确保s1比s2大（减少计算）
    if (s2.len() > s1.len()) {
        std::swap(s1, s2);
    }

    // Step 1: 为每个线程计算子范围
    std::vector<SegmentS> s1s(nt), s2s(nt), sws(nt);
    s2s[0].i0 = s2.i0;
    s2s[nt - 1].i1 = s2.i1;

    // Step 2: 为s1的每个分割点在s2中找到对应的分割点
#pragma omp parallel for num_threads(nt)
    for (int t = 0; t < nt; t++) {
        s1s[t].i0 = s1.i0 + s1.len() * t / nt;
        s1s[t].i1 = s1.i0 + s1.len() * (t + 1) / nt;

        if (t + 1 < nt) {
            // 在s2中二分查找分割点
            T pivot = src[s1s[t].i1];
            size_t i0 = s2.i0, i1 = s2.i1;
            while (i0 + 1 < i1) {
                size_t imed = (i1 + i0) / 2;
                if (comp(pivot, src[imed])) {
                    i1 = imed;
                } else {
                    i0 = imed;
                }
            }
            s2s[t].i1 = s2s[t + 1].i0 = i1;
        }
    }

    // Step 3: 计算目标范围
    s1.i0 = std::min(s1.i0, s2.i0);
    s1.i1 = std::max(s1.i1, s2.i1);
    s2 = s1;
    sws[0].i0 = s1.i0;
    for (int t = 0; t < nt; t++) {
        sws[t].i1 = sws[t].i0 + s1s[t].len() + s2s[t].len();
        if (t + 1 < nt) {
            sws[t + 1].i0 = sws[t].i1;
        }
    }

    // Step 4: 执行并行归并
#pragma omp parallel for num_threads(nt)
    for (int t = 0; t < nt; t++) {
        SegmentS sw = sws[t];
        SegmentS s1t = s1s[t];
        SegmentS s2t = s2s[t];

        if (s1t.i0 < s1t.i1 && s2t.i0 < s2t.i1) {
            for (;;) {
                if (comp(src[s1t.i0], src[s2t.i0])) {
                    dst[sw.i0++] = src[s1t.i0++];
                    if (s1t.i0 == s1t.i1) break;
                } else {
                    dst[sw.i0++] = src[s2t.i0++];
                    if (s2t.i0 == s2t.i1) break;
                }
            }
        }

        // 复制剩余元素
        if (s1t.len() > 0) {
            memcpy(dst + sw.i0, src + s1t.i0, s1t.len() * sizeof(dst[0]));
        } else if (s2t.len() > 0) {
            memcpy(dst + sw.i0, src + s2t.i0, s2t.len() * sizeof(dst[0]));
        }
    }
}
```

**算法复杂度**:
- **二分查找**: O(nt × log(|s2|))
- **并行归并**: O((|s1| + |s2|) / nt)
- **总体**: O(n/p × log n) 其中p为处理器数

**关键优化**:
1. **非对称处理**: 确保s1 >= s2，减少二分查找次数
2. **内存局部性**: 使用memcpy批量复制剩余元素
3. **负载均衡**: 通过二分查找确保每个线程处理相等的工作量

---

## 3. 桶排序优化

### 3.1 基础桶排序

**算法流程** (sorting.cpp:212-249):

```cpp
void bucket_sort_ref(
        size_t nval,
        const uint64_t* vals,
        uint64_t vmax,
        int64_t* lims,       // 输出: 桶边界 [vmax+1]
        int64_t* perm) {     // 输出: 排列数组
    // Step 1: 计数 (直方图)
    memset(lims, 0, sizeof(*lims) * (vmax + 1));
    for (size_t i = 0; i < nval; i++) {
        FAISS_THROW_IF_NOT(vals[i] < vmax);
        lims[vals[i] + 1]++;
    }

    // Step 2: 累加和
    for (size_t i = 0; i < vmax; i++) {
        lims[i + 1] += lims[i];
    }
    FAISS_THROW_IF_NOT(lims[vmax] == nval);

    // Step 3: 填充桶
    for (size_t i = 0; i < nval; i++) {
        perm[lims[vals[i]]++] = i;
    }

    // Step 4: 重置指针
    for (size_t i = vmax; i > 0; i--) {
        lims[i] = lims[i - 1];
    }
    lims[0] = 0;
}
```

**内存布局**:

```
输入: vals = [3, 1, 3, 0, 2, 1]
      vmax = 4

Step 1 (计数):
lims = [0, 0, 0, 0, 0]
      -> [1, 2, 1, 2, 0]  (0出现1次, 1出现2次, ...)

Step 2 (累加):
lims = [0, 1, 3, 4, 6]

Step 3 (填充):
perm = [3, 1, 4, 0, 2, 5]
       (vals[perm[i]]是非递减的)

Step 4 (重置):
lims = [0, 1, 3, 4, 6]
桶0: perm[0:1]   = [3]
桶1: perm[1:3]   = [1, 5]
桶2: perm[3:4]   = [4]
桶3: perm[4:6]   = [0, 2]
```

### 3.2 并行桶排序

**并行化策略** (sorting.cpp:251-328):

```cpp
void bucket_sort_parallel(
        size_t nval,
        const uint64_t* vals,
        uint64_t vmax,
        int64_t* lims,
        int64_t* perm,
        int nt_in) {
    memset(lims, 0, sizeof(*lims) * (vmax + 1));

#pragma omp parallel num_threads(nt_in)
    {
        int nt = omp_get_num_threads();
        int rank = omp_get_thread_num();
        std::vector<int64_t> local_lims(vmax + 1);

        // 分配范围
        size_t i0 = nval * rank / nt;
        size_t i1 = nval * (rank + 1) / nt;

        // Phase 1: 构建本地直方图
        for (size_t i = i0; i < i1; i++) {
            local_lims[vals[i]]++;
        }

        // Phase 2: 累加到全局直方图
#pragma omp critical
        {
            for (size_t i = 0; i < vmax; i++) {
                lims[i + 1] += local_lims[i];
            }
        }

#pragma omp barrier

        // Phase 3: 计算累加和 (主线程)
#pragma omp master
        {
            for (size_t i = 0; i < vmax; i++) {
                lims[i + 1] += lims[i];
            }
        }

#pragma omp barrier

        // Phase 4: 分配写入位置
#pragma omp critical
        {
            for (size_t i = 0; i < vmax; i++) {
                size_t nv = local_lims[i];
                local_lims[i] = lims[i];     // 记录起始位置
                lims[i] += nv;               // 保留空间
            }
        }

#pragma omp barrier

        // Phase 5: 填充桶
        for (size_t i = i0; i < i1; i++) {
            perm[local_lims[vals[i]]++] = i;
        }

#pragma omp barrier

        // Phase 6: 重置lims
#pragma omp master
        {
            for (size_t i = vmax; i > 0; i--) {
                lims[i] = lims[i - 1];
            }
            lims[0] = 0;
        }
    }
}
```

**并行化分析**:

| 阶段 | 操作 | 并行方式 | 复杂度 |
|------|------|----------|--------|
| 1 | 本地直方图 | 独立 | O(n/p) |
| 2 | 累加 | critical | O(vmax/p) |
| 3 | 累加和 | master | O(vmax) |
| 4 | 分配位置 | critical | O(vmax/p) |
| 5 | 填充桶 | 独立 | O(n/p) |
| 6 | 重置 | master | O(vmax) |

其中p为线程数，vmax为桶数。

**性能优化技巧**:
1. **减少锁竞争**: 使用本地直方图减少critical区域
2. **缓存友好**: 每个线程处理连续的内存区域
3. **批量操作**: Phase 3和6使用master线程串行执行

---

## 4. 原地桶排序

### 4.1 原地排序算法

**问题**: 当需要在排序后原地保留值时，不能使用额外的perm数组。

**解决方案**: 使用循环置换（cycle-following）算法 (sorting.cpp:335-400):

```cpp
template <class TI>
void bucket_sort_inplace_ref(
        size_t nrow,
        size_t ncol,
        TI* vals,          // nrow × ncol矩阵，原地排序
        TI nbucket,
        int64_t* lims) {
    size_t nval = nrow * ncol;

    // Step 1-2: 计算直方图和累加和
    memset(lims, 0, sizeof(*lims) * (nbucket + 1));
    for (size_t i = 0; i < nval; i++) {
        lims[vals[i] + 1]++;
    }
    for (size_t i = 0; i < nbucket; i++) {
        lims[i + 1] += lims[i];
    }

    // Step 3: 初始化指针
    std::vector<size_t> ptrs(nbucket);
    for (size_t i = 0; i < nbucket; i++) {
        ptrs[i] = lims[i];
    }

    // Step 4: 循环置换
    TI row = -1;                  // 当前处理的行号
    TI init_bucket_no = 0;        // 初始化桶号
    TI bucket_no = 0;             // 当前桶号

    for (;;) {
        size_t idx = ptrs[bucket_no];

        if (row >= 0) {
            ptrs[bucket_no]++;     // 推进指针
        }

        TI next_bucket_no = vals[idx];
        vals[idx] = row;           // 存储row号

        if (next_bucket_no != -1) {
            // 继续当前循环
            row = idx / ncol;
            bucket_no = next_bucket_no;
        } else {
            // 开始新循环
            for (; init_bucket_no < nbucket; init_bucket_no++) {
                if (ptrs[init_bucket_no] < lims[init_bucket_no + 1]) {
                    break;
                }
            }
            if (init_bucket_no == nbucket) {
                break;  // 完成
            }
            bucket_no = init_bucket_no;
            row = -1;
        }
    }
}
```

**循环置换示例**:

```
初始: vals = [3, 1, 3, 0, 2, 1], ncol = 1
lims = [0, 1, 3, 4, 6]
ptrs = [0, 1, 3, 4, 6]

循环1 (bucket_no=0):
  idx=0, vals[0]=3, next_bucket_no=3
  vals[0]=-1, ptrs[0]=1
  -> row=0, bucket_no=3

循环1继续 (bucket_no=3):
  idx=4, vals[4]=2, next_bucket_no=2
  vals[4]=0, ptrs[3]=5
  -> row=4, bucket_no=2

循环1继续 (bucket_no=2):
  idx=3, vals[3]=0, next_bucket_no=0
  vals[3]=4, ptrs[2]=4
  -> row=3, bucket_no=0

循环1完成 (bucket_no=0, ptrs[0]=1 != lims[1]):
  idx=1, vals[1]=1, next_bucket_no=1
  vals[1]=3, ptrs[0]=2
  -> row=1, bucket_no=1

... (继续直到所有元素归位)

最终: vals = [0, 1, 2, 3, 4, 5] (row号，按桶排序)
```

### 4.2 并行原地桶排序

**挑战**: 多个线程同时执行循环置换会相互干扰。

**解决方案**: 使用分阶段写入策略 (sorting.cpp:457-642):

```cpp
template <class TI>
void bucket_sort_inplace_parallel(
        size_t nrow,
        size_t ncol,
        TI* vals,
        TI nbucket,
        int64_t* lims,
        int nt_in) {
    memset(lims, 0, sizeof(*lims) * (nbucket + 1));
    std::vector<ToWrite<TI>> all_to_write;

    // 限制内存使用 (< 5GB)
    size_t init_to_write = std::max(
            size_t(nbucket),
            std::min(nval / 10,
                    ((size_t)5 << 30) / (sizeof(TI) * 3 * nt_in)));

    std::vector<size_t> ptrs(nbucket);
    std::vector<char> did_wrap(nbucket);

#pragma omp parallel num_threads(nt_in)
    {
        int nt = omp_get_num_threads();
        int rank = omp_get_thread_num();
        std::vector<int64_t> local_lims(nbucket + 1);

        // Phase 1: 本地直方图
        size_t i0 = nval * rank / nt;
        size_t i1 = nval * (rank + 1) / nt;
        for (size_t i = i0; i < i1; i++) {
            local_lims[vals[i]]++;
        }

#pragma omp critical
        {
            for (size_t i = 0; i < nbucket; i++) {
                lims[i + 1] += local_lims[i];
            }
            all_to_write.push_back(ToWrite<TI>(nbucket));
        }

#pragma omp barrier
        ToWrite<TI>& to_write = all_to_write[rank];

#pragma omp master
        {
            // Phase 2: 累加和
            for (size_t i = 0; i < nbucket; i++) {
                lims[i + 1] += lims[i];
            }

            // 初始化ptrs
            memcpy(ptrs.data(), lims, sizeof(lims[0]) * nbucket);

            // 预填充初始元素
            size_t written = 0;
            for (TI b = 0; b < nbucket; b++) {
                size_t l0 = lims[b], l1 = lims[b + 1];
                size_t target_to_write = l1 * init_to_write / nval;
                do {
                    if (l0 == l1) break;
                    to_write.add(-1, b);  // 标记为空位
                    l0++;
                    written++;
                } while (written < target_to_write);
            }

            to_write.bucket_sort();
        }

        // Phase 3: 分配桶范围
        size_t b0 = (rank * nbucket + nt - 1) / nt;
        size_t b1 = ((rank + 1) * nbucket + nt - 1) / nt;

        // Phase 4: 迭代式写入
        int round = 0;
        for (;;) {
#pragma omp barrier

            // 检查完成条件
            size_t n_to_write = 0;
            for (const ToWrite<TI>& tw : all_to_write) {
                n_to_write += tw.lims.back();
            }

            if (n_to_write == 0) break;

#pragma omp barrier

            ToWrite<TI> next_to_write(nbucket);

            // 写入当前批次并收集被覆盖的元素
            for (size_t b = b0; b < b1; b++) {
                for (const ToWrite<TI>& tw : all_to_write) {
                    size_t l0 = tw.lims[b];
                    size_t l1 = tw.lims[b + 1];
                    for (size_t i = l0; i < l1; i++) {
                        TI row = tw.rows[i];
                        size_t idx = ptrs[b];

                        if (idx < lims[b + 1]) {
                            ptrs[b]++;
                        } else {
                            // 回绕
                            assert(!did_wrap[b]);
                            did_wrap[b] = true;
                            idx = lims[b];
                            ptrs[b] = idx + 1;
                        }

                        // 记录被覆盖的元素
                        if (vals[idx] >= 0) {
                            TI new_row = idx / ncol;
                            next_to_write.add(new_row, vals[idx]);
                        }

                        vals[idx] = row;
                    }
                }
            }

            next_to_write.bucket_sort();
#pragma omp barrier
            all_to_write[rank].swap(next_to_write);
            round++;
        }
    }
}
```

**ToWrite辅助结构** (sorting.cpp:404-454):

```cpp
template <class TI>
struct ToWrite {
    TI nbucket;
    std::vector<TI> buckets;   // 目标桶号
    std::vector<TI> rows;      // 要写入的行号
    std::vector<size_t> lims;  // 桶边界

    explicit ToWrite(TI nbucket) : nbucket(nbucket) {
        lims.resize(nbucket + 1);
    }

    void add(TI row, TI b) {
        rows.push_back(row);
        buckets.push_back(b);
    }

    void bucket_sort() {
        // 对buckets进行排序，生成lims
        memset(lims.data(), 0, sizeof(lims[0]) * (nbucket + 1));
        for (size_t i = 0; i < buckets.size(); i++) {
            lims[buckets[i] + 1]++;
        }
        for (size_t i = 0; i < nbucket; i++) {
            lims[i + 1] += lims[i];
        }

        // 排序rows
        std::vector<TI> new_rows(rows.size());
        std::vector<size_t> ptrs = lims;
        for (size_t i = 0; i < buckets.size(); i++) {
            TI b = buckets[i];
            new_rows[ptrs[b]++] = rows[i];
        }
        buckets.resize(0);
        std::swap(rows, new_rows);
    }

    void swap(ToWrite& other) {
        buckets.swap(other.buckets);
        rows.swap(other.rows);
        lims.swap(other.lims);
    }
};
```

---

## 5. 并行哈希表

### 5.1 哈希表设计

**数据结构** (sorting.h:71-76):

```cpp
// 表结构: 2 * (1 << log2_capacity) 个int64
// tab[2*i]   = key
// tab[2*i+1] = value

void hashtable_int64_to_int64_init(int log2_capacity, int64_t* tab);
void hashtable_int64_to_int64_add(
        int log2_capacity, int64_t* tab,
        size_t n, const int64_t* keys, const int64_t* vals);
void hashtable_int64_to_int64_lookup(
        int log2_capacity, const int64_t* tab,
        size_t n, const int64_t* keys, int64_t* vals);
```

**设计目标**:
- **批量操作**: 支持批量添加和查找
- **并行处理**: 多线程安全
- **冲突处理**: 开放寻址法

### 5.2 哈希函数

**实现** (sorting.cpp:703-705):

```cpp
namespace {
// https://bigprimes.org/
int64_t bigprime = 8955327411143;

inline int64_t hash_function(int64_t x) {
    return (x * 1000003) % bigprime;  // 乘法哈希
}
}
```

**乘法哈希特点**:
- **良好分布**: 乘数1000003是质数
- **快速计算**: 单次乘法和取模
- **低冲突**: 使用大质数bigprime取模

### 5.3 桶数量计算

**自适应桶数** (sorting.cpp:694-698):

```cpp
int log2_capacity_to_log2_nbucket(int log2_capacity) {
    return log2_capacity < 12    ? 0
            : log2_capacity < 20 ? log2_capacity - 12
                                 : 10;
}
```

**策略**:

| log2_capacity | log2_nbucket | nbucket | 说明 |
|---------------|--------------|---------|------|
| < 12 | 0 | 1 | 小容量，单桶 |
| 12-19 | capacity-12 | 2^(capacity-12) | 中等，线性增长 |
| >= 20 | 10 | 1024 | 大容量，固定桶数 |

### 5.4 批量添加

**实现** (sorting.cpp:718-782):

```cpp
void hashtable_int64_to_int64_add(
        int log2_capacity,
        int64_t* tab,
        size_t n,
        const int64_t* keys,
        const int64_t* vals) {
    size_t capacity = (size_t)1 << log2_capacity;
    int64_t mask = capacity - 1;
    int log2_nbucket = log2_capacity_to_log2_nbucket(log2_capacity);
    size_t nbucket = (size_t)1 << log2_nbucket;

    // Phase 1: 计算哈希值和桶号
    std::vector<int64_t> hk(n);
    std::vector<uint64_t> bucket_no(n);

#pragma omp parallel for
    for (int64_t i = 0; i < n; i++) {
        hk[i] = hash_function(keys[i]) & mask;
        bucket_no[i] = hk[i] >> (log2_capacity - log2_nbucket);
    }

    // Phase 2: 按桶号排序
    std::vector<int64_t> lims(nbucket + 1);
    std::vector<int64_t> perm(n);
    bucket_sort(
            n, bucket_no.data(), nbucket,
            lims.data(), perm.data(),
            omp_get_max_threads());

    // Phase 3: 每个桶独立插入
    int num_errors = 0;
#pragma omp parallel for reduction(+ : num_errors)
    for (int64_t bucket = 0; bucket < nbucket; bucket++) {
        size_t k0 = bucket << (log2_capacity - log2_nbucket);
        size_t k1 = (bucket + 1) << (log2_capacity - log2_nbucket);

        for (size_t i = lims[bucket]; i < lims[bucket + 1]; i++) {
            int64_t j = perm[i];
            size_t slot = hk[j];

            // 开放寻址: 线性探测
            for (;;) {
                if (tab[slot * 2] == -1) {
                    // 找到空位，插入
                    tab[slot * 2] = keys[j];
                    tab[slot * 2 + 1] = vals[j];
                    break;
                } else if (tab[slot * 2] == keys[j]) {
                    // 键已存在，覆盖
                    tab[slot * 2 + 1] = vals[j];
                    break;
                }

                slot++;
                if (slot == k1) {
                    slot = k0;  // 回绕
                }
                if (slot == hk[j]) {
                    // 桶满了
                    num_errors++;
                    break;
                }
            }
        }
    }

    FAISS_THROW_IF_NOT_MSG(num_errors == 0,
                            "hashtable capacity exhausted");
}
```

**算法流程**:

```
输入: keys = [k0, k1, k2, ...]
      vals = [v0, v1, v2, ...]

Phase 1: 计算哈希
hk[i] = hash_function(keys[i]) & mask
bucket_no[i] = hk[i] >> (log2_capacity - log2_nbucket)

Phase 2: 桶排序
按bucket_no排序，获得perm

Phase 3: 并行插入
for each bucket (并行):
    for i in lims[bucket]:lims[bucket+1]:
        j = perm[i]
        在桶[bucket]范围内插入 keys[j], vals[j]
```

### 5.5 批量查找

**实现** (sorting.cpp:784-825):

```cpp
void hashtable_int64_to_int64_lookup(
        int log2_capacity,
        const int64_t* tab,
        size_t n,
        const int64_t* keys,
        int64_t* vals) {
    size_t capacity = (size_t)1 << log2_capacity;
    std::vector<int64_t> hk(n), bucket_no(n);
    int64_t mask = capacity - 1;
    int log2_nbucket = log2_capacity_to_log2_nbucket(log2_capacity);

#pragma omp parallel for
    for (int64_t i = 0; i < n; i++) {
        int64_t k = keys[i];
        hk[i] = hash_function(k) & mask;
        bucket_no[i] = hk[i] >> (log2_capacity - log2_nbucket);
    }

    // 并行查找
    // (注意: 没有显式排序，因为查找是O(1)操作)
    for (int64_t i = 0; i < n; i++) {
        int64_t k = keys[i];
        size_t slot = hk[i];

        if (tab[2 * slot] == -1) {
            // 桶为空
            vals[i] = -1;
        } else if (tab[2 * slot] == k) {
            // 首次命中
            vals[i] = tab[2 * slot + 1];
        } else {
            // 线性探测
            size_t bucket = bucket_no[i];
            size_t k0 = bucket << (log2_capacity - log2_nbucket);
            size_t k1 = (bucket + 1) << (log2_capacity - log2_nbucket);

            for (;;) {
                if (tab[slot * 2] == k) {
                    vals[i] = tab[slot * 2 + 1];
                    break;
                }
                slot++;
                if (slot == k1) {
                    slot = k0;
                }
                if (slot == hk[i]) {
                    // 未找到
                    vals[i] = -1;
                    break;
                }
            }
        }
    }
}
```

**性能分析**:

| 操作 | 平均复杂度 | 最坏复杂度 |
|------|-----------|-----------|
| 添加 | O(1 + α) | O(n) |
| 查找 | O(1 + α) | O(n) |

其中α = n / capacity是负载因子。

---

## 6. 距离计算器集成

### 6.1 堆操作集成

**与CMax/CMin的集成** (faiss/utils/Heap.h):

```cpp
// 最小堆操作 (用于L2距离)
template <typename T>
inline void heap_pop_min(
        size_t k,
        T* bh_val,
        int64_t* bh_ids) {
    // 使用CMax比较器 (最小堆)
    // ...
}

// 最大堆操作 (用于内积)
template <typename T>
inline void heap_pop_max(
        size_t k,
        T* bh_val,
        int64_t* bh_ids) {
    // 使用CMin比较器 (最大堆)
    // ...
}
```

### 6.2 通用的堆操作

**使用C模板** (Heap.h):

```cpp
template <class C>
inline void heap_replace_top(
        size_t k,
        typename C::T* bh_val,
        typename C::TI* bh_ids,
        typename C::T val,
        typename C::TI id) {
    // C可以是CMax或CMin
    // 统一的堆替换逻辑
    bh_val--;  // 使用1-based索引
    bh_ids--;
    // ... 堆下沉操作
}
```

**调用示例**:

```cpp
// L2距离: 使用CMax (最小堆)
float distances[10];
int64_t labels[10];
heap_replace_top<CMax<float, int64_t>>(
        10, distances, labels, new_distance, new_label);

// 内积: 使用CMin (最大堆)
heap_replace_top<CMin<float, int64_t>>(
        10, distances, labels, new_distance, new_label);
```

---

## 性能优化技巧总结

### 1. 比较操作优化

| 技巧 | 说明 | 示例 |
|------|------|------|
| 编译期多态 | 使用模板而非虚函数 | `C::cmp(a, b)` |
| 内联函数 | 编译期展开 | `inline static bool cmp` |
| 类型traits | 编译期条件选择 | `std::conditional` |

### 2. 并行排序优化

| 技术 | 效果 | 实现 |
|------|------|------|
| 分段归并 | 减少锁竞争 | `parallel_merge` |
| 二分查找 | 负载均衡 | pivot查找分割点 |
| 批量复制 | 内存局部性 | `memcpy`剩余元素 |

### 3. 桶排序优化

| 技术 | 效果 | 实现 |
|------|------|------|
| 本地直方图 | 减少锁竞争 | `local_lims` |
| 循环置换 | 原地操作 | `bucket_sort_inplace` |
| 分阶段写入 | 并行安全 | `ToWrite`结构 |

### 4. 哈希表优化

| 技术 | 效果 | 实现 |
|------|------|------|
| 桶排序 | 批量处理 | `bucket_sort` |
| 开放寻址 | 缓存友好 | 线性探测 |
| 乘法哈希 | 快速计算 | `hash_function` |

---

## 参考资料

1. **相关源码**:
   - `faiss/utils/ordered_key_value.h` - 比较器模板
   - `faiss/utils/sorting.{h,cpp}` - 排序算法
   - `faiss/utils/Heap.h` - 堆操作
   - `faiss/utils/extra_distances-inl.h` - 距离计算器

2. **算法参考**:
   - [Parallel Merge Algorithm](https://en.wikipedia.org/wiki/Merge_algorithm#Parallel_merge)
   - OpenMP并行编程规范
   - C++ STL排序算法实现

---

*本文档详细剖析了Faiss中比较、排序和哈希表的底层实现，包括CMax/CMin比较模板、并行归并排序、桶排序和并行哈希表的C++优化细节。*
